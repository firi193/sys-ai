# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 1: Foundation

## Current Goal

- Move to the next feature spec.

## Completed

- Feature spec 01: Design system — shadcn/ui initialized, all 7 UI primitives added (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), lucide-react installed, lib/utils.ts with cn() created, globals.css updated with full dark theme token set.
- Feature spec 02: Editor chrome — `components/editor/editor-navbar.tsx` (fixed h-12 navbar, sidebar toggle with PanelLeftOpen/PanelLeftClose, left/center/right sections, dark bg + bottom border) and `components/editor/project-sidebar.tsx` (floating overlay z-50, slides from left without pushing content, Projects header + close button, My Projects / Shared tabs with empty placeholders, full-width New Project button). Dialog pattern ready via existing shadcn Dialog component and globals.css tokens.
- Feature spec 03: Auth — `proxy.ts` at project root wires Clerk middleware (all routes protected except public sign-in/sign-up paths from env vars); `ClerkProvider` wraps root layout with `@clerk/ui` dark theme and CSS variable overrides (no hardcoded colors); `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` use two-panel layout (left: logo + tagline + feature list, hidden on small screens; right: Clerk form); root `/` redirects authenticated users to `/editor` and unauthenticated to `/sign-in`; `UserButton` added to editor navbar right section. `@clerk/ui` installed.
- Feature spec 04: Project dialogs — `hooks/use-project-dialogs.ts` manages dialog/form/loading state; `components/editor/project-dialogs-context.tsx` provides context to layout tree; `CreateProjectDialog` (name input + live slug preview), `RenameProjectDialog` (prefilled, auto-focus, Enter submits), `DeleteProjectDialog` (destructive confirm, no input) added to editor layout; sidebar updated with rename/delete actions on owned projects (hidden for shared), mobile backdrop scrim via `md:hidden` overlay; editor home screen (`app/editor/page.tsx`) shows heading, description, and New Project button wired to Create dialog. Mock project data only.
- Feature spec 05: Prisma data models — `prisma/models/project.prisma` defines `Project` (ownerId→Clerk, name, optional description, `DRAFT`/`ARCHIVED` status, `canvasJsonPath`, timestamps, indexes on ownerId+createdAt) and `ProjectCollaborator` (project FK cascade delete, email, createdAt, unique on projectId+email, indexes on email and projectId+date). `lib/prisma.ts` exports a cached singleton: branches on DATABASE_URL prefix (`prisma+postgres://` → `accelerateUrl`, otherwise `@prisma/adapter-pg`). Client generated to `app/generated/prisma`. Migration pending database connectivity.
- Feature spec 06: Project APIs — `app/api/projects/route.ts` (GET lists owner's projects ordered by createdAt desc; POST creates with defaulted name "Untitled Project"); `app/api/projects/[projectId]/route.ts` (PATCH renames, DELETE deletes). Both mutation routes enforce 401 for unauthenticated and 403 for non-owner. `npm run build` passes.
- Feature spec 07: Wire editor home — `lib/data/projects.ts` server-side helper fetches owned projects (by ownerId) and shared projects (by collaborator email via `currentUser()`); `app/editor/layout.tsx` converted to async server component, fetches project lists and passes to `EditorShell`; `components/editor/editor-shell.tsx` client shell holds sidebar toggle state and provides `ProjectDialogsContext`; `hooks/use-project-actions.ts` manages dialog state and mutations (create: slugify + unique suffix → roomId → POST /api/projects → navigate; rename: PATCH → refresh; delete: DELETE → redirect if active workspace else refresh); `POST /api/projects` updated to accept optional client-generated `id`; `app/editor/page.tsx` converted to server component using `NewProjectButton` client leaf; dialogs wired to real handlers with loading states. `npm run build` passes.
- Feature spec 08: Editor workspace shell — `lib/project-access.ts` provides `getCurrentUserIdentity()` (Clerk userId + primary email) and `getProjectWithAccess()` (checks owner or collaborator); `components/editor/access-denied.tsx` centered layout with lock icon, message, and link back to `/editor`; `app/editor/[roomId]/page.tsx` server component: unauthenticated → redirect `/sign-in`, missing/unauthorized project → `AccessDenied`, authorized → `WorkspaceShell`; `components/editor/workspace-shell.tsx` full-viewport overlay (`fixed inset-0 z-50`) with workspace navbar (project name, Share button, AI sidebar toggle, UserButton), `ProjectSidebar` with `activeProjectId` highlighting current room, canvas placeholder, and collapsible AI sidebar placeholder; `ProjectSidebar` updated with optional `activeProjectId` prop to highlight active project. `npm run build` passes.
- Feature spec 09: Share dialog — `lib/collaborators.ts` lists a project's `ProjectCollaborator` rows and enriches each by email via Clerk Backend API (`clerkClient().users.getUserList({ emailAddress })`), falling back to email-only when no Clerk user matches; `app/api/projects/[projectId]/collaborators/route.ts` (GET: owner-or-collaborator can list; POST: owner-only invite, validates email format, rejects inviting own email, 409 on duplicate via Prisma `P2002`); `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` (DELETE: owner-only remove, scoped to projectId). `hooks/use-share-collaborators.ts` owns dialog open state and all collaborator actions (`openDialog` fetches on open, `invite`, `remove`, `copyLink` with temporary `copied` flag); `components/editor/share-dialog.tsx` is a presentational dialog (owner sees invite input + remove buttons, collaborators see read-only list, avatar falls back to initials when no Clerk image). `app/editor/[roomId]/page.tsx` now computes `isOwner` (`project.ownerId === identity.userId`) and passes it to `WorkspaceShell`, which instantiates `useShareCollaborators` and wires the navbar Share button to `share.openDialog`. `npm run build` and `npm run lint` pass.
- Feature spec 10: Liveblocks setup — `liveblocks.config.ts` types `Presence` (`cursor: {x,y} | null`, `isThinking: boolean`) and `UserMeta.info` (`name`, `avatar`, `color`); unused `Storage`/`RoomEvent`/`ThreadMetadata`/`RoomInfo` placeholders changed from `{}` to `Record<string, never>` to satisfy `@typescript-eslint/no-empty-object-type`. `@liveblocks/node` installed. `lib/liveblocks.ts` exports `getLiveblocksClient()` — a globally cached singleton (same pattern as `lib/prisma.ts`), constructed lazily on first call (not at module load) so a missing `LIVEBLOCKS_SECRET_KEY` doesn't break `next build`'s page-data collection — and `getCursorColorForUser(userId)`, a deterministic hash-to-palette mapper (9-color fixed palette). `app/api/liveblocks-auth/route.ts`: requires Clerk auth via `getCurrentUserIdentity()` (401 if absent), reads `roomId` from the request body's `room` field (Liveblocks client posts project ID as room), verifies access via `getProjectWithAccess()` (403 if denied), calls `getOrCreateRoom(roomId, { defaultAccesses: [] })`, then issues an access-token session (`prepareSession` + `session.allow(roomId, session.FULL_ACCESS)`) carrying `userInfo: { name, avatar, color }` sourced from Clerk's `currentUser()` and `getCursorColorForUser`. Access-token model chosen (over ID tokens) because authorization is already fully resolved by our own DB-backed ownership/collaborator check — the token only needs to grant the single verified room, not rely on Liveblocks-side room permissions. `LIVEBLOCKS_SECRET_KEY` env var not yet set locally; route is unauthenticated against the real Liveblocks API until configured. `npm run build` and `npm run lint` pass.
- Feature spec 11: Base canvas — `types/canvas.ts` (`CanvasNodeShape` union of the 6 shapes, `CanvasNodeData` with `label`/`color`/`shape` — extends `Record<string, unknown>` so it satisfies `@xyflow/react`'s `Node<T>` data constraint, `CanvasNode = Node<CanvasNodeData, "canvasNode">`, `CanvasEdge = Edge<Record<string, never>, "canvasEdge">`) already existed from spec 10 groundwork and needed only the `Record<string, unknown>` fix. `components/editor/canvas.tsx` (client) calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ nodes: { initial: [] }, edges: { initial: [] }, suspense: true })` from `@liveblocks/react-flow` and renders `ReactFlow` with `connectionMode={ConnectionMode.Loose}`, `fitView`, dot-pattern `Background`, and `MiniMap`; imports `@xyflow/react/dist/style.css` directly in the component file (external stylesheets are importable anywhere under `app/` in this Next.js version, not just the root layout). `components/editor/canvas-room.tsx` (client) sets up the room: a local `CanvasErrorBoundary` class component (no `react-error-boundary` dependency — Suspense doesn't catch connection errors, so a boundary is needed) wraps `LiveblocksProvider` (`authEndpoint="/api/liveblocks-auth"`) → `RoomProvider` (`id={roomId}`, `initialPresence={{ cursor: null, isThinking: false }}` — `isThinking` is required by the `Presence` type from spec 10 even though the spec only calls out `cursor`) → `ClientSideSuspense` (simple "Loading canvas…" fallback) → `Canvas`. `components/editor/workspace-shell.tsx` now renders `<CanvasRoom roomId={roomId} />` in place of the "Canvas coming soon" placeholder; `<main>` changed to `relative flex-1` (no more centering) so the canvas fills the available space — `Canvas` wraps `ReactFlow` in a `h-full w-full` div since React Flow requires an ancestor with a definite height and the provider/suspense layers in between render no DOM. Scope intentionally excludes: `Controls`, custom node/edge rendering, persistence, AI behavior. `npm run build` and `npm run lint` pass.
- Feature spec 12: Shape panel — `types/canvas.ts` extended with the `NODE_COLORS`/`NODE_SHAPES` constants that `context/ui-context.md` already documented as living there: `NODE_COLORS` (8 named fill/text pairs, `DEFAULT_NODE_COLOR = NODE_COLORS.neutral`) and `NODE_SHAPES` (`NodeShapeDefinition[]` pairing each `CanvasNodeShape` with a Lucide icon — `RectangleHorizontal`, `Diamond`, `Circle`, `Pill`, `Cylinder`, `Hexagon` — and a default `{ width, height }`; rectangle 160×80, diamond 140×140, circle 100×100, pill 140×60, cylinder 100×120, hexagon 120×100). `components/editor/shape-panel.tsx` (client) renders a floating pill toolbar (`absolute inset-x-0 bottom-6`, rounded-full surface) with one draggable icon button per `NODE_SHAPES` entry; `onDragStart` writes `{ shape, width, height }` as JSON under a custom `application/x-canvas-shape` MIME type (exported as `SHAPE_DRAG_MIME`, payload typed as `ShapeDragPayload`). `components/editor/canvas.tsx` restructured: `Canvas` now wraps a new inner `CanvasFlow` component in `<ReactFlowProvider>` (required because `useReactFlow()` — needed for `screenToFlowPosition` — only works in a descendant of the provider, not in the same component that renders `<ReactFlow>`); the wrapper `div` around `<ReactFlow>` gets `onDragOver` (calls `preventDefault`, sets `dropEffect = "move"`) and `onDrop` handlers; on drop, the shape payload is read via `SHAPE_DRAG_MIME`, the pointer position is converted with `screenToFlowPosition({ x: clientX, y: clientY })`, a node ID is built as `` `${shape}-${Date.now()}-${container}` `` where `container` is a `useRef`-backed incrementing counter (guards against duplicate IDs from same-millisecond drops), and the new node (`label: ""`, `color: DEFAULT_NODE_COLOR.fill`, the dragged `shape`) is added via `onNodesChange([{ type: "add", item: newNode }])` — not `useReactFlow().setNodes`, since nodes are controlled by Liveblocks Storage and only `onNodesChange` (from `useLiveblocksFlow`) writes through to the synced room; `nodeTypes={{ canvasNode: CanvasNodeRenderer }}` registered as a module-level constant (avoids React Flow's re-creation warning). `npm run build` and `npm run lint` pass. Not verified interactively in a browser: no Clerk session/browser automation available in this environment, and a separate dev server was already running locally under the user's own session — ask the user to confirm the drag-drop-render loop visually in that session.
- Fix (`context/current-issues.md`): all dropped shapes were rendering as plain bordered rectangles regardless of the dragged shape. Root cause — `components/editor/canvas-node.tsx` from spec 12 was intentionally shape-agnostic (one bordered `div` for every shape) as a placeholder, and nothing had implemented the shape-specific visuals `context/ui-context.md` calls for. Added `getNodeTextColor(fill)` to `types/canvas.ts` (looks up the matching `NODE_COLORS` pair by fill, falls back to `DEFAULT_NODE_COLOR.text`). Rewrote `canvas-node.tsx` to switch on `data.shape`: `rectangle` keeps the bordered `div` (small radius); `circle`/`pill` both use a `rounded-full` div (stadium shape naturally falls out of `rounded-full` on a non-square box); `diamond`/`hexagon`/`cylinder` render as inline SVG per `ui-context.md` (`polygon` for diamond/hexagon, a body `path` + top `ellipse` for cylinder), each using `vectorEffect="non-scaling-stroke"` so the 1px border doesn't distort under the `preserveAspectRatio="none"` stretch, and stroke color sourced from `var(--border-default)` via inline `style` (CSS vars aren't reliably resolved in raw SVG presentation attributes, but are in `style`). Label is an absolutely-positioned centered `span` on top of the shape in every case, colored via `getNodeTextColor`. `npm run build` and `npm run lint` pass.
- Feature spec 13: Node shape rendering + drag preview — extracted the per-shape visual (rectangle/pill/circle as CSS `div`s, diamond/hexagon/cylinder as inline SVG) out of `canvas-node.tsx` into a new shared `components/editor/shape-visual.tsx` (`ShapeVisual({ shape, color, strokeColor })`), so the same shape-rendering logic can back both the live node and the drag preview instead of duplicating it. `canvas-node.tsx` now reads `selected` from `NodeProps` and passes `strokeColor: selected ? "var(--accent-primary)" : "var(--border-default)"` into `ShapeVisual` — borders stay subtle at rest and brighten to the brand cyan when a node is selected. SVG shapes already scaled with node size via `preserveAspectRatio="none"` + `h-full w-full` (unchanged, carried over from the earlier fix). Drag preview: `components/editor/shape-panel.tsx` renders one hidden `ShapeVisual` per `NODE_SHAPES` entry, sized to that shape's `defaultSize`, inside a `fixed left-[-9999px] top-[-9999px]` container (off-screen but still painted — `display:none`/`opacity:0` elements don't work as native drag images in Chromium) and keeps refs to each in a `Partial<Record<CanvasNodeShape, HTMLDivElement | null>>`. `onDragStart` calls the native `event.dataTransfer.setDragImage(preview, width/2, height/2)` so the browser's built-in drag-image renderer shows the correct shape/size centered on the cursor and automatically follows the pointer and disappears on drop or cancel — no custom mousemove tracking or extra state needed. Node creation on drop, the shape panel layout, and resize/label editing were left untouched per scope limits. `npm run build` and `npm run lint` pass.
- Fix (`context/current-issues.md`): dropped shapes couldn't be connected to each other. Root cause — `canvas-node.tsx`'s custom `canvasNode` type never rendered any `Handle` components; React Flow (even with `onConnect` wired and `connectionMode={ConnectionMode.Loose}` set on `<ReactFlow>`) has nothing to start or end a connection drag on without them. Added four `Handle`s (`Position.Top/Right/Bottom/Left`, `type="source"`, ids matching position — `isConnectableStart`/`isConnectableEnd` default `true` so each works bidirectionally under Loose mode) to `CanvasNodeRenderer`, styled per `context/ui-context.md` (white, `8px`, subtle border) and hidden at rest / revealed on node hover via a `group`/`group-hover:opacity-100` class on the wrapping `div` (inline `style` used for handle color/size so it reliably overrides `@xyflow/react`'s default dark handle color, which a same-specificity Tailwind class can't guarantee given import order). `npm run build` and `npm run lint` pass. `context/current-issues.md` cleared.
- Feature spec 14: Node resizing + inline label editing — `types/canvas.ts` adds `MIN_NODE_SIZE` (`{ width: 48, height: 40 }`) next to `CanvasNodeSize`. `canvas-node.tsx`: added `<NodeResizer isVisible={selected} minWidth minHeight handleStyle lineStyle />` (from `@xyflow/react`) inside `CanvasNodeRenderer` — resize handles are cyan-on-dark (`var(--accent-primary)` border on a `var(--bg-elevated)` fill) to read as subtle but on-brand, and only show when the node is selected; dimension changes flow through `NodeResizer` → React Flow's internal `triggerNodeChanges` → the same `onNodesChange` from `useLiveblocksFlow` already wired in `canvas.tsx`, which `@liveblocks/react-flow` handles via its built-in `"dimensions"` case (writes synced top-level `width`/`height` onto the storage node) — no new sync code needed. Inline label editing: double-clicking the node body swaps the centered `NodeLabel` span for a `LabelEditor` textarea absolutely positioned in the same `inset-0` box (no layout shift); the textarea carries `nodrag nopan` classes (React Flow's default drag/pan-suppression classes) so typing, selecting text, or dragging the cursor inside it never moves the node or pans the canvas. Both the label span and the textarea show a muted "Label" placeholder (`var(--text-faint)`) when `data.label` is empty. Because nodes are Liveblocks-storage-controlled (not React Flow's own `setNodes`/`updateNodeData`, which only mutate an internal batch that a controlled `nodes` prop immediately overwrites), label commits are dispatched as `onNodesChange([{ id, type: "replace", item: { ...node, data: { ...node.data, label } } }])` from a new `updateLabel` callback defined in `canvas.tsx` (has access to the full canonical node from `useLiveblocksFlow`'s `nodes` array, required because `NodeReplaceChange` reconciles with `extent: "full"` — omitting any existing top-level key like `style`/`width`/`height` would delete it); passed down to `CanvasNodeRenderer` via a new `CanvasNodeActionsContext`/`CanvasNodeActionsProvider` (plain React context, not node `data`, since `data` is synced to storage and can't carry a function) that `canvas.tsx` provides around `<ReactFlow>`. Every keystroke calls `updateLabel` immediately (live-syncs to collaborators as you type, per spec), while a local `draftLabel` state keeps typing responsive independent of the Storage round-trip; blur commits (stops editing, keeps the last-typed value); `Escape` reverts to the label captured when editing started and stops editing. Shape rendering, the shape panel, and drop-to-create were untouched per scope limits.
- Fix: double-click-to-edit did nothing (couldn't select or type into the label at all). Root cause — React Flow's `<ReactFlow>` defaults `zoomOnDoubleClick` to `true`, wiring `d3-zoom`'s own `dblclick.zoom` listener directly onto the pane DOM element, which sits *below* React's delegated synthetic-event listener in the DOM tree. `d3-zoom`'s handler calls `event.stopImmediatePropagation()` on the native event on every double-click that isn't inside a `.nopan`-classed element, which fired before the event could ever bubble up to reach our `onDoubleClick` on the node — so `startEditing` never ran and the textarea never mounted. Fixed by adding `zoomOnDoubleClick={false}` to `<ReactFlow>` in `canvas.tsx` (double-click-to-zoom wasn't relied on anywhere else). Also fixed a real but secondary issue found in the same pass: `@xyflow/react`'s stylesheet sets `user-select: none` on every `.react-flow__node`, which is inherited by the textarea and blocks mouse-drag text selection inside it (typing/focus are unaffected by `user-select`, only highlighting) — fixed by adding `select-text` plus an explicit inline `userSelect: "text"` directly on the `<textarea>` in `canvas-node.tsx` (a direct rule on the element itself always wins over an inherited value, regardless of the ancestor rule's specificity or stylesheet order). `npm run build` and `npm run lint` pass.
- Not verified interactively in a browser (no Clerk session/browser automation in this environment, and a separate dev server was already running locally under the user's own session, per the same caveat as spec 12) — ask the user to confirm in that session: selecting a node shows corner/edge resize handles that respect the minimum size, resizing persists after deselecting/reselecting, double-clicking a node's label opens a smoothly-overlaid textarea (no jump) that can be typed into and text-selected with the mouse, typing updates the label live, blur/Escape close editing as expected (Escape discards the in-progress edit), dragging/panning don't trigger while typing in the textarea, and double-clicking elsewhere on the canvas no longer zooms in.
- Feature spec 16: Canvas ergonomics — control bar, keyboard shortcuts — new `hooks/use-keyboard-shortcuts.ts` (`useKeyboardShortcuts({ reactFlowInstance, undo, redo })`) attaches a single `window` `keydown` listener: ignores events when `event.target` is an `<input>`/`<textarea>`/`isContentEditable` element; `Cmd/Ctrl+Z` undoes (or redoes with Shift held), `Cmd/Ctrl+Y` redoes, `+`/`=` zoom in, `-` zooms out (all `preventDefault`ed so the browser's own zoom/undo doesn't fire); zoom keys call `reactFlowInstance.zoomIn`/`zoomOut` with a `duration` so keyboard zoom animates the same as the button. New `components/editor/canvas-control-bar.tsx` (`CanvasControlBar`) renders a pill toolbar (same visual pattern as `shape-panel.tsx`: `rounded-full border-surface-border bg-surface` pill, circular icon buttons) positioned `absolute bottom-6 left-6 z-20` — bottom-left, and stacked above the shape panel's `z-10` per the spec's "sit above the shape panel" (the shape panel itself stays bottom-center per the spec's own "don't change the shape panel" limit, so the two pills sit side by side without overlapping); zoom group (`ZoomOut`/`Maximize`/`ZoomIn` from lucide) calls `reactFlowInstance.zoomOut`/`fitView`/`zoomIn` directly, each with the same animation `duration`; a `bg-surface-border` divider (1px) separates it from the history group (`Undo2`/`Redo2`), which is disabled (`disabled:opacity-40 disabled:pointer-events-none`) when `canUndo`/`canRedo` is false. `canvas.tsx`: `CanvasFlow` now calls Liveblocks' room-scoped `useUndo`/`useRedo`/`useCanUndo`/`useCanRedo` from `@liveblocks/react` directly (this project's `liveblocks.config.ts` only declares global `Liveblocks` types, not a `createRoomContext` — these hooks resolve the room from React context at call time, same as the existing `useLiveblocksFlow` pattern already in this file, so no new context/provider was needed); `useReactFlow()`'s return value is now held in a `reactFlowInstance` variable (previously destructured immediately to only `screenToFlowPosition`) so the same instance can be passed to both `useKeyboardShortcuts` and `<CanvasControlBar>`; `<MiniMap />` removed from inside `<ReactFlow>` per the spec's step 6. Scope limits honored: shape panel, node/edge rendering, and the Liveblocks room/provider setup in `canvas-room.tsx` were left untouched; no extra canvas controls were added beyond the zoom/history pairs and their shortcuts. `npm run build` and `npm run lint` pass (lint's 2 warnings are pre-existing/unrelated, in `lib/liveblocks.ts` and `lib/prisma.ts`).
- Not verified interactively in a browser (same environment caveat as prior specs — no Clerk session/browser automation available here) — ask the user to confirm in their own session: the control bar appears bottom-left without overlapping the shape panel, zoom in/out/fit view animate smoothly and match the `+`/`-` keyboard shortcuts, undo/redo buttons visibly dim and stop responding to clicks when there's nothing to undo/redo, `Cmd/Ctrl+Z`/`Shift+Z`/`Y` drive the same Liveblocks history as the buttons, and none of the shortcuts fire while typing in a node's label textarea (`use-keyboard-shortcuts.ts`'s editable-target check).
- Feature spec 15: Node color toolbar — the predefined color palette (`NODE_COLORS`, 8 fill/text pairs) and text-color lookup (`getNodeTextColor`) already existed in `types/canvas.ts` from spec 12/the current-issues fix, so no new palette or type work was needed there. New `components/editor/node-color-toolbar.tsx` (`NodeColorToolbar`) renders a floating pill of swatch buttons — one per `NODE_COLORS` entry — positioned with `absolute bottom-full left-1/2 -translate-x-1/2 mb-2` on top of the node's own `relative` wrapper, so it sits just above the node without overlapping and re-centers automatically as the node resizes; carries `nodrag nopan` plus `onMouseDown`/`onClick` `stopPropagation` so clicking a swatch cannot start a node drag, pan the canvas, or bubble into a pane-click deselect. Each swatch is a small circle filled with the pair's fill color; the active swatch (matching the node's current `data.color`) gets a `2px` border plus a small always-on box-shadow in the pair's text color, inactive swatches get a subtle `var(--border-default)` border; hovering any swatch adds a tight `0 0 6px` box-shadow keyed to that swatch's own text color via an inline `--glow-color` CSS custom property (can't be expressed as a static Tailwind class since the color is per-swatch and data-driven). `canvas-node.tsx`: `CanvasNodeActions` context gains `updateColor(id, color)` alongside the existing `updateLabel`; `CanvasNodeRenderer` renders `<NodeColorToolbar activeColor={data.color} onSelect={...} />` only `{selected ? ... : null}`, wired to a `handleColorSelect` that calls `updateColor(id, pair.fill)`. `canvas.tsx` adds a `updateColor` callback mirroring `updateLabel`'s existing pattern exactly (look up the canonical node from `useLiveblocksFlow`'s `nodes`, dispatch `onNodesChange([{ id, type: "replace", item: { ...node, data: { ...node.data, color } } }])` — same reasoning as spec 14: nodes are Liveblocks-storage-controlled, so only `onNodesChange` writes through to the synced room) and passes it into `CanvasNodeActionsProvider`. Text color needed no separate update path — `CanvasNodeRenderer` already derives it every render via `getNodeTextColor(data.color)`, so changing only the stored fill color automatically repaints the label in its paired text color for every collaborator, live, with no server calls. Scope limits honored: no changes to drag/drop, no changes to node selection logic, no full color picker — palette is fixed to the 8 predefined pairs. `npm run build` and `npm run lint` pass.
- Feature spec 17: Presence avatars + cursors — `liveblocks.config.ts`'s `Presence.isThinking` renamed to `thinking` to match the field name the spec defines (`cursor: {x,y}|null`, `thinking: boolean`); `components/editor/canvas-room.tsx`'s `initialPresence` updated to match. New `components/editor/presence-avatars.tsx` (client) renders the collaborator group: reads `useOthers()` (Liveblocks) and `useUser()` (Clerk), filters out any entry whose `other.id` equals the current Clerk user ID (defensive/explicit per spec, even though `useOthers()` already excludes the local connection), renders up to 5 overlapping avatars (`-space-x-2`, `ring-2 ring-base` for the background-colored cutout separation that keeps overlapping avatars readable against the dark canvas) with a `+N` chip beyond that, a `bg-surface-border` divider shown only when at least one collaborator exists, and a same-sized (`h-8 w-8`, via `UserButton`'s `appearance.elements.userButtonAvatarBox`) Clerk `UserButton` last — display-only, no click handlers on the collaborator avatars. New `components/editor/live-cursors.tsx` (client) renders other participants' cursors as a `fixed inset-0` pointer-events-none overlay: reads `other.presence.cursor` (flow-space coordinates) and converts to viewport coordinates per-render via `useReactFlow().flowToScreenPosition` (subscribing to `useViewport()` to force a re-render — and thus a fresh conversion — on every pan/zoom tick, since `flowToScreenPosition`'s output depends on the current transform); pointer SVG + name badge both colored from `other.info.color` (the same deterministic per-user color `lib/liveblocks.ts` assigns at auth time), falling back to `var(--accent-primary)`/`"Anonymous"` if info is momentarily absent. `components/editor/canvas.tsx`: added `useMyPresence` (from `@liveblocks/react`) and `onPointerMove`/`onPointerLeave` handlers on the existing outer canvas div (same div that already had `onDragOver`/`onDrop`) that broadcast `{ cursor: screenToFlowPosition({x: clientX, y: clientY}) }` on move and `{ cursor: null }` on leave — chosen over Liveblocks' own prebuilt `@liveblocks/react-flow` `Cursors` component because that component resolves each cursor's name/color via `useUser()`, which requires a `resolveUsers` callback on `LiveblocksProvider` (unset here) — without it, `useUser()` returns `error: missingUserError` and renders with no name/color, silently failing the spec's "match pointer/badge color to presence color" requirement; hand-rolling against `other.info` (already present on the room's `others` array from the access-token `userInfo`, no extra resolver needed) avoids that gap. `<LiveCursors />` and `<PresenceAvatars />` added to `CanvasFlow`'s existing overlay stack alongside `<CanvasControlBar />`/`<ShapePanel />` (top-right corner, `z-20`/`z-30`, `pointer-events-none` wrappers with `pointer-events-auto` on the actual interactive bits — same pattern already used by those two components). `components/editor/editor-navbar.tsx` (used only by the editor home shell, `editor-shell.tsx`) was left untouched — it is already a separate component from `workspace-shell.tsx`'s inline canvas-room nav, so the presence UI naturally only appears in the canvas/room view per the spec's scope limit. `npm run build` and `npm run lint` pass.

- Feature spec 19: Canvas autosave — `app/api/projects/[projectId]/canvas/route.ts` (`PUT`/`GET`): both handlers require `getCurrentUserIdentity()` (401) and `getProjectWithAccess()` (403), matching the existing route pattern; `PUT` validates the body is `{ nodes: [], edges: [] }` (400 otherwise), uploads via `@vercel/blob`'s `put("canvas/{projectId}.json", ..., { access: "public", addRandomSuffix: false, allowOverwrite: true, contentType: "application/json" })` — a fixed, overwritable pathname per project (per `architecture-context.md`'s documented `canvas/{projectId}.json` pattern) rather than blob's default random-suffixed/versioned naming, since only the latest snapshot per project is ever needed — then writes the returned blob URL onto the existing `Project.canvasJsonPath` field (reused as-is from spec 05, no schema change); `GET` reads `canvasJsonPath` off the project record and re-fetches the JSON from that blob URL, returning `{ canvas: null }` when no save exists yet or the blob fetch fails. New `hooks/use-canvas-autosave.ts` (`useCanvasAutosave(projectId, nodes, edges)`): a `useEffect` keyed on `[projectId, nodes, edges]` clears any pending timeout and schedules a new 1.5s-debounced `PUT` on every nodes/edges change — since Liveblocks storage already gives referentially-stable arrays that only change identity on real mutations, no extra diffing was needed; a `skipFirstRef` guard no-ops the very first effect run (mount) so autosave never fires before the user makes any change, avoiding a redundant write of freshly-loaded (or genuinely empty) state; returns a `CanvasSaveStatus` (`"idle" | "saving" | "saved" | "error"`) for the caller to surface. `components/editor/canvas.tsx`: `CanvasFlow` now takes `projectId`/`onSaveStatusChange` props, calls `useCanvasAutosave` and forwards status changes up via a `useEffect`; a separate one-shot load effect (`hasAttemptedLoadRef` guard, deps intentionally limited to `[projectId]` since it must run exactly once per mount regardless of subsequent nodes/edges changes) checks `nodes.length > 0 || edges.length > 0` — since `useLiveblocksFlow`'s `suspense: true` guarantees storage is already resolved by first render, this check is race-free — and only when the room is empty does it `GET` the canvas route and replay the saved nodes/edges through `onNodesChange`/`onEdgesChange` as `"add"` changes (the same Storage-mutating path already used elsewhere in this file), per the spec's "skip the load entirely to avoid overwriting active collaboration" rule. `Canvas`/`CanvasRoom` (`canvas-room.tsx`) now thread `projectId`/`onSaveStatusChange` down from `WorkspaceShell`, which passes `roomId` as `projectId` (the room ID and project ID are the same value throughout this codebase — confirmed via `app/editor/[roomId]/page.tsx`, which calls `getProjectWithAccess(roomId, identity)` directly). `workspace-shell.tsx` adds a `saveStatus` state and a small `SaveStatusIndicator` (no prior "Save" button existed in the navbar, so one was added per the spec's step 5 — improvised as a status-only, non-interactive indicator rather than a manual-save trigger, since the spec only calls for autosave, not a manual save action) between the project name and the Share button: `Save`/`Loader2` (spinning)/`Check`/`AlertCircle` icons colored `text-copy-muted`/`text-copy-muted`/`text-success`/`text-error` for idle/saving/saved/error respectively, using the existing `--state-success`/`--state-error` tokens (`text-success`/`text-error` Tailwind utilities). `npm run build` and `npm run lint` pass.
- Feature spec 18: AI sidebar shell — new `components/editor/ai-sidebar.tsx` (`AISidebar({ isOpen, onClose })`, client) extracted the AI sidebar out of `workspace-shell.tsx`'s inline placeholder `<aside>` into its own floating component, following the same fixed-overlay + translate slide pattern already used by `project-sidebar.tsx` (`fixed top-12 right-0 h-[calc(100vh-3rem)] w-96 border-l border-surface-border bg-base/95 shadow-2xl transition-transform duration-300`, `translate-x-0`/`translate-x-full`) rather than the previous conditionally-rendered flex sibling, which had no slide animation and pushed the canvas width — matches `ui-context.md`'s documented layout pattern ("slide-over AI sidebar on the right" as a floating overlay, mirroring the left `ProjectSidebar`). Header: small `Bot` icon, `AI Workspace` title (`text-copy-primary`), `Collaborate with Ghost AI` subtitle (`text-copy-muted`), close button. Tabbed layout via shadcn `Tabs` (`AI Architect` / `Specs`); active tab styled `data-active:bg-ai data-active:text-ai-text` — resolved the spec's placeholder token names (`bg-accent`/`text-accent`/`text-accent-text`) to this project's actual `--accent-ai`/`--accent-ai-text` ("AI accent"/"AI text") tokens from `ui-context.md`, since no literal `accent`/`accent-text` Tailwind class exists in `globals.css`'s `@theme inline` mapping and the AI-specific token pair is the closest semantic match for an "AI Workspace" surface; inactive tab text `text-copy-muted`. AI Architect tab: `ScrollArea`-based scrollable message list; empty state (`Bot` icon, description, three starter-prompt pill buttons styled `bg-subtle text-ai-text` that populate a message when clicked); user messages right-aligned `bg-brand-dim border-2 border-brand/50 text-copy-primary`; assistant messages left-aligned `bg-elevated border border-surface-border text-ai-text`; `Textarea` input (`min-h-[72px] max-h-[160px]`, auto-resizes via the shadcn `Textarea`'s existing `field-sizing-content` base class — no extra JS resize logic needed) with `Enter` submits / `Shift+Enter` newline, send button `bg-ai text-white`. Sending a message locally appends the user's text plus one hardcoded placeholder assistant reply ("Ghost AI generation isn't wired up yet.") purely to demonstrate both message stylings — no AI call, no backend, matching the spec's Specs-tab "demo card for now" precedent and the explicit "don't add AI generation logic yet" scope limit. Specs tab: `Generate Spec` button (`bg-ai text-white`, no handler) above a static demo spec card (`bg-elevated border-surface-border`, `FileText` icon, demo title/snippet, disabled `Download` button with `Download` icon). `workspace-shell.tsx` now renders `<AISidebar isOpen={isAISidebarOpen} onClose={...} />` as a fixed-position sibling inside the canvas row (same slot the old inline `<aside>` occupied) instead of a flex child — open/close state ownership (`isAISidebarOpen` in `WorkspaceShell`) and the navbar toggle button were left untouched per scope limits. `npm run build` and `npm run lint` pass (lint's 2 warnings are the same pre-existing/unrelated ones in `lib/liveblocks.ts`/`lib/prisma.ts` noted under spec 16).

- Fix (`context/current-issues.md`): selected nodes/edges couldn't be deleted. New `hooks/use-canvas-delete.ts` (`useCanvasDelete({ onDelete })`) reads live selection state via `@xyflow/react`'s `useNodes()`/`useEdges()` and attaches a single `window` `keydown` listener (same `isEditableTarget` input/textarea/contenteditable guard pattern as `use-keyboard-shortcuts.ts`, duplicated rather than shared to avoid touching that unrelated file): on `Delete`/`Backspace`, filters to `selected` nodes/edges and calls the existing `onDelete` mutation from `useLiveblocksFlow`. First attempt wrongly dispatched `{ id, type: "remove" }` through `onNodesChange`/`onEdgesChange` — traced (after the user reported delete still not working) to `@liveblocks/react-flow`'s `flow.js`, where `applyNodeChanges`/`applyEdgeChanges` treat the `"remove"` change type as a no-op (`case "remove": break;`); the library only removes storage entries through the dedicated `onDelete` mutation, which takes `{ nodes, edges }` arrays of full node/edge objects and deletes them from the underlying `LiveMap`s directly — switched the hook to call `onDelete({ nodes: selectedNodes, edges: selectedEdges })` instead. `components/editor/canvas.tsx`: calls the hook from `CanvasFlow` with the same `onDelete` already destructured from `useLiveblocksFlow` and passed to `<ReactFlow onDelete={onDelete}>`; `<ReactFlow>` also sets `deleteKeyCode={null}` to fully disable React Flow's own built-in keyboard deletion path per the issue's explicit instruction.
- Fix (`context/current-issues.md`): removed `<UserButton />` from the workspace navbar (`components/editor/workspace-shell.tsx`'s inline `<nav>`) — the editor home navbar (`components/editor/editor-navbar.tsx`, used only by `editor-shell.tsx`) keeps its own `<UserButton />` untouched. Note: the issue described "the navbar" as shared between the two contexts requiring a conditional-render prop, but the codebase already has two separate navbar implementations (workspace-shell.tsx's inline nav vs. the standalone `EditorNavbar` component) — no shared component exists, so no conditional logic was needed to achieve the stated outcome (`UserButton` gone from workspace, present on editor home).
- `npm run build` and `npm run lint` pass for both fixes above (lint's 2 warnings are the same pre-existing/unrelated ones in `lib/liveblocks.ts`/`lib/prisma.ts`).
- Feature spec 20: Design agent API (backend task wiring, no AI logic) — `prisma/models/task-run.prisma` adds `TaskRun` (`runId` unique — a unique constraint already provisions the "index on runId" the spec calls out as a separate bullet, so no redundant `@@index([runId])` was added alongside it; `projectId`, `userId`, `createdAt`; compound index on `[userId, projectId]`); migration `20260706214303_add_task_run` created and applied (`DATABASE_URL` in `.env.local` was reachable this session, unlike spec 05's pending-connectivity precedent). `trigger/design-agent.ts` exports `designAgent` via the existing `task()` pattern already established in `trigger/example.ts` (same `@trigger.dev/sdk` import, no new setup) — accepts `{ prompt, roomId }`, calls `logger.log` with the payload spread into a plain object (`logger.log`'s second arg is typed `Record<string, unknown>`, which a `payload` typed as a named interface doesn't structurally satisfy without the spread), and returns the payload unchanged; no AI provider calls, no node/edge generation. `app/api/ai/design/route.ts` (`POST`): requires `getCurrentUserIdentity()` (401) and `getProjectWithAccess(projectId, identity)` (403) — same ownership-check pattern as every other mutation route in this codebase, satisfying the architecture invariant that auth/ownership is enforced at every mutation boundary even though the spec's route description didn't spell out project-access verification explicitly; triggers `designAgent.trigger({ prompt, roomId })`, persists a `TaskRun` row (`runId` from the returned run handle's `.id`, `projectId`, `userId`), returns `{ runId }`. `app/api/ai/design/token/route.ts` (`POST`): requires auth (401), looks up the `TaskRun` by the submitted `runId`, verifies `taskRun.userId === identity.userId` (403 otherwise — ownership check is against the `TaskRun` record itself per the spec, not a second project-access lookup), then calls `@trigger.dev/sdk`'s `auth.createPublicToken({ scopes: { read: { runs: [runId] } } } )` (imported as `triggerAuth` to avoid shadowing Clerk's `auth`) and returns `{ token }`. Scope limits honored: no node/edge generation, no AI provider calls, no canvas updates — task only logs/echoes its input. `npm run build` and `npm run lint` pass (same 2 pre-existing/unrelated warnings).

- Feature spec 21: Design agent logic — `trigger/design-agent.ts` rewritten to actually generate and apply a design instead of echoing its payload; the API routes from spec 20 (`app/api/ai/design`, `app/api/ai/design/token`) and the `TaskRun` model were left untouched since the spec's Implementation section scopes this unit to the task file only. Investigated `@liveblocks/react-flow`'s bundled `./node` subpath export (`node_modules/@liveblocks/react-flow/dist/node.d.ts`) before implementing per the spec's "check Liveblocks and Trigger.dev agent skills" instruction — no dedicated Liveblocks/Trigger skill exists in this environment, so the equivalent was reading the packages' own bundled docs/type declarations (same pattern `AGENTS.md` establishes for `node_modules/next/dist/docs`): found `mutateFlow({ client, roomId }, callback)`, an official server-side companion to the client's `useLiveblocksFlow` — it opens the same default `"flow"`-keyed Liveblocks Storage `LiveObject` (matching `canvas.tsx`'s un-overridden `storageKey`) and exposes `addNode`/`updateNode`/`updateNodeData`/`removeNode`/`addEdge`/`removeEdge` — so the task mutates the canvas through the exact same Storage schema the client reads, with zero new state system or canvas architecture changes (satisfies both scope limits). Two `mutateFlow` calls per run: one to snapshot `flow.nodes`/`flow.edges` as plain arrays (read-only use of the same API — no mutation performed), and a second to apply the AI's actions after generation. Gemini call: `createGoogleGenerativeAI({ apiKey: process.env.GEMINI_API_KEY })` from `@ai-sdk/google` — used the explicit-apiKey constructor form rather than the bare `google` default export because `@ai-sdk/google` only auto-reads `GOOGLE_GENERATIVE_AI_API_KEY` from the environment by default, not the `GEMINI_API_KEY` name the spec's Dependencies section says is already configured. `generateObject` (from `ai`) with a `zod` discriminated-union schema (`addNode`/`moveNode`/`resizeNode`/`updateNodeData`/`deleteNode`/`addEdge`/`deleteEdge`, matching the spec's action list exactly) drives structured output against model `gemini-2.5-flash`; `zod` (already present in `node_modules` only as an auto-installed peer dependency of `ai`/`@ai-sdk/google`, not previously a direct dependency of this project) added explicitly to `package.json` since `design-agent.ts` now imports it directly. Design-rule enforcement split between prompt guidance and hard validation: node shape and color are constrained to `NODE_SHAPES`/`NODE_COLORS` via `z.enum` (the model literally cannot emit an invalid value), while layout/spacing (no fixed pixel rule exists in `ui-context.md` beyond default node sizes) is enforced by prompting the model with the current canvas's node/edge snapshot plus explicit spacing/no-overlap instructions rather than a server-side collision-avoidance algorithm — judged proportionate since spacing is a soft/visual concern and the current-state-aware prompt is what lets the model correctly reference existing node/edge ids for move/resize/update/delete/connect actions in the first place. New nodes reuse `NODE_SHAPES[shape].defaultSize` (not model-supplied) so shape sizing stays canonical; `resizeNode`/new-node sizes are floored to `MIN_NODE_SIZE`. AI activity: `getLiveblocksClient().broadcastEvent(roomId, { type: "ai-status", runId, status, message })` at `start`/`processing`/`complete`/`error` (caught and re-thrown after publishing an `"error"` status so the run still fails loudly in the Trigger.dev dashboard) — required extending `liveblocks.config.ts`'s `RoomEvent` (previously an unused `Record<string, never>` placeholder from spec 10) with this event shape so `broadcastEvent` is properly typed; no UI currently subscribes to `RoomEvent`, so this is wired but not yet rendered anywhere (see Next Up). AI presence: `getLiveblocksClient().setPresence(roomId, { userId: "ghost-ai", data: { cursor, thinking }, userInfo: { name: "Ghost AI", color: "#6457f9" }, ttl })` — chosen over a websocket-connected client because `@liveblocks/node`'s `setPresence` is a REST call purpose-built for exactly this ("useful for scenarios like showing an AI agent's presence in a room," per its own doc comment), requiring no persistent connection from the background task; because its `data` shape (`{ cursor, thinking }`) matches this project's existing `Presence` type from spec 10/17 exactly, the AI's presence is picked up automatically and rendered by the *already-existing* `LiveCursors` and `PresenceAvatars` components (both iterate `useOthers()` and read `other.presence.cursor` / `other.info.color` / `other.info.name` with no knowledge of who the "other" is) — so Ghost AI shows up as a live cursor and a collaborator avatar in every connected client with zero canvas/component changes, fulfilling "AI presence... visible to all participants" without touching canvas architecture. Presence is refreshed (`thinking: true`, `ttl: 90`) at task start and again after generation (cursor set to the first action's position, giving a rough "the AI is working here" signal), then cleared in a `finally` block via one last `setPresence` call with `ttl: 2` (the API's minimum) since `@liveblocks/node` has no explicit "delete presence" method — an expiring TTL is the documented way to make ephemeral presence disappear. `npm run build` and `npm run lint` pass (same 2 pre-existing/unrelated warnings). `npx trigger.dev deploy --dry-run` fails, but on a pre-existing, unrelated `@trigger.dev/react-hooks@^4.5.1` vs. installed CLI `4.5.0` version-mismatch check — present before this change and outside this spec's `npm run build`-based check-when-done criteria.

- Feature spec 22: AI presence + shared status feed (UI/presence only, no AI generation wiring) — investigated `node_modules/@liveblocks/{core,react,node}/dist/*.d.ts` (no dedicated Liveblocks skill file covers this newer primitive) and found a real, typed Liveblocks **Feed** API (`useFeeds`/`useFeedMessages`/`useCreateFeed`/`useCreateFeedMessage` in `@liveblocks/react`, `createFeed`/`createFeedMessage`/`getFeedMessages` in `@liveblocks/node`, `FeedMessage<FMD> = { id, createdAt, updatedAt, data: FMD }`) — a first-class, persisted, subscribable primitive distinct from the ephemeral `broadcastEvent`/`RoomEvent` mechanism spec 21 used; per the spec's "follow Liveblocks best practices for feeds/presence instead of creating parallel realtime state" this is what "reuse a Liveblocks feed named `ai-status-feed`" refers to. `types/tasks.ts` (new) defines `AI_STATUS_FEED_ID = "ai-status-feed"`, `AIStatusFeedMessageData` (`{ text?: string }`, deliberately minimal/generic per the spec's "keep the feed generic enough for design and spec generation later"), and a runtime type guard `isAIStatusFeedMessageData` — the boundary validation the spec's step 3 calls for. `liveblocks.config.ts` registers `FeedMessageData: AIStatusFeedMessageData` on the global `Liveblocks` interface (a plain interface with no index signature satisfies the `Json`-constrained `GetOverride` check the same way the existing `RoomEvent` field already does — confirmed by the pre-existing build passing) so `useFeedMessages`/`useCreateFeedMessage` are typed automatically project-wide, matching the `type-liveblocks-correctly` pattern. `trigger/design-agent.ts` (the actual publisher of AI status, from spec 21) was deliberately left untouched — wiring it to `createFeedMessage` is "UI changes and background task changes" combined in one step per `ai-workflow-rules.md`'s split rule, and is out of scope until a future spec adds the real client trigger point; this spec only builds the read/subscribe side, so the sidebar currently has nothing to display until that future wiring exists. `components/editor/canvas-room.tsx`: `CanvasRoom` gained an optional `children` prop rendered as a sibling of `<ClientSideSuspense>` inside `<RoomProvider>` (not inside the Suspense boundary, since presence/feed hooks don't need Storage to be ready) — required because `AISidebar` needs the same Liveblocks room context Presence/Feed hooks resolve from, and it was previously rendered as a flex sibling of `<CanvasRoom>` entirely outside any `RoomProvider`. `components/editor/workspace-shell.tsx` now nests `<AISidebar>` as `<CanvasRoom>`'s child instead of a sibling — visually unchanged, since `AISidebar` is `fixed`-positioned and no ancestor between it and the viewport sets a CSS transform/filter that would create a new containing block. `components/editor/ai-sidebar.tsx`: new `useAIActivity()` hook — `isAIThinking` is derived from `useOthers().some(other => other.presence.thinking)` (the same `Presence.thinking` field spec 17/21 already established, not a new state channel); a one-time `useEffect` calls `useCreateFeed()("ai-status-feed")` and swallows the rejection (idempotent — safe if the feed already exists or another client's create call wins the race), guaranteeing `useFeedMessages` doesn't hit `FEED_NOT_FOUND` before any status has ever been published; `useFeedMessages(AI_STATUS_FEED_ID)`'s latest message is picked by `reduce`-ing on `createdAt` rather than assuming array order (the type declarations don't document a sort order, so this is order-independent by construction) and validated through `isAIStatusFeedMessageData` before its `text` is trusted. `AISidebar` renders a status banner (spinner + `statusText`, falling back to "Ghost AI is working…" when thinking with no message text yet) between the header and the tabs, shown whenever either signal is present; `AIArchitectTab` takes a new `disabled` prop (`= isAIThinking`) that disables the `Textarea`, the send `Button` (swapping its icon to a spinning `Loader2`), and the starter-prompt chips — the rest of the sidebar (tab switching, Specs tab) stays interactive per the spec's "keep the rest of the sidebar usable" limit. `components/editor/live-cursors.tsx`: the cursor name badge now renders a small `animate-spin` `Loader2` before the name whenever `other.presence.thinking` is true (hidden otherwise, including when `thinking` is the initial `false`) — no new presence field needed, since `thinking` already existed on `Presence` from spec 17/21. Scope limits honored: no AI generation logic, no background task triggering, no full feed history rendered (only the single latest message), the rest of the sidebar was not blocked/dimmed. `npm run build` and `npm run lint` pass (same 2 pre-existing/unrelated warnings).

- Feature spec 23: Sidebar chat feed — added a second, separate Liveblocks feed (`ai-chat`, `AI_CHAT_FEED_ID` in `types/tasks.ts`) for room-scoped human collaborator chat, kept distinct from `ai-status-feed`. `types/tasks.ts` adds `ChatFeedMessageData` (`sender`, `role: "user" | "assistant"`, `content`, `timestamp`) and `isChatFeedMessageData`, a runtime guard backed by a Zod schema (`chatFeedMessageSchema`, declared `satisfies z.ZodType<ChatFeedMessageData>` so it can't silently drift from the interface). Discovered along the way that Liveblocks only supports **one global `FeedMessageData` type for every feed in a room** (`useFeedMessages`/`useCreateFeedMessage` aren't generic per feed id) — a real `AIStatusFeedMessageData | ChatFeedMessageData` union in `liveblocks.config.ts` fails Liveblocks' own `Json`-value constraint check (confirmed via an isolated `tsc` repro: this turned out to be a pre-existing structural-typing gap in this Liveblocks version — *any* named interface, even the already-shipped single-field `AIStatusFeedMessageData` alone, fails that internal `extends Json` check and silently resolves `FeedMessage.data` to an unusable error-string type; the existing spec-22 status feed code only ever worked around it by accident, via `isAIStatusFeedMessageData`'s type-guard *intersection*-narrowing at each access site rather than an upfront `.filter()`). Fixed by widening `types/tasks.ts`'s new `RoomFeedMessageData` (the `FeedMessageData` override in `liveblocks.config.ts`) to a single flat interface with every feed's fields declared optional — still satisfies the same broken check no worse than before, and gives each feed's type guard something to narrow from at read time. `components/editor/ai-sidebar.tsx`'s `useChatFeed()` follows the same working idiom as `useAIActivity()`: reads `useFeedMessages(AI_CHAT_FEED_ID)`, and instead of `.filter()` with a type predicate (which silently no-ops here since a clean `ChatFeedMessage` type can't structurally extend the broken `FeedMessage<ErrorType>` element type), uses `.flatMap()` to pull each validated message's fields out individually right after the `isChatFeedMessageData` check, producing a properly-typed `ChatFeedMessage[]` with no residual error-type leakage, sorted by `timestamp`. A one-time `useCreateFeed(AI_CHAT_FEED_ID)` effect (same idempotent-losing-the-race pattern as the status feed) ensures the feed exists. `AIArchitectTab` (previously spec 18's local-only fake conversation with a hardcoded placeholder assistant reply) now sends/reads real messages through this feed instead: `useSelf()` supplies the sender's display name (`self?.info?.name`, already populated at Liveblocks auth time via Clerk — no separate Clerk fetch needed); `sendMessage` (wrapped in `useCallback` — a plain arrow function tripped ESLint's `react-hooks/purity` rule on the `Date.now()` timestamp call, which this codebase's other `Date.now()`/`Math.random()` call sites avoid by always living inside a `useCallback` or a non-component helper function) posts `{ sender, role: "user", content, timestamp: Date.now() }` via `useCreateFeedMessage()`, clears the draft on success, and sets a local `sendError` string on failure (rendered as a small `text-error` line under the input, reusing the same token `workspace-shell.tsx`'s save-status indicator uses); own vs. others' messages are distinguished by `sender === senderName` for the existing right/left bubble styling (brand vs. AI-accent) carried over from spec 18. The starter-prompt chips now send real chat messages instead of triggering the old fake-assistant echo. No AI replies, no backend task triggering, and no mixing with `ai-status-feed` — its subscription/publish path in `useAIActivity()` is untouched. `npm run build` and `npm run lint` pass (lint's 2 warnings are the same pre-existing/unrelated ones in `lib/liveblocks.ts`/`lib/prisma.ts`).

- Feature spec 24: Design agent frontend wiring — `components/editor/ai-sidebar.tsx` gains a new local `useDesignAgentRun(roomId)` hook (same file-local pattern as the existing `useAIActivity`/`useChatFeed`) that is the actual client trigger point spec 21/22's notes flagged as missing: on submit it `POST`s `/api/ai/design` with `{ prompt, roomId, projectId: roomId }` (the two routes from spec 20 require `projectId` separately even though this spec's own request shape only names `roomId` — adapted rather than changed, since `roomId` and `projectId` are confirmed the same value everywhere else in this codebase, e.g. spec 19's notes, and touching the route contracts would be backend work this spec is scoped to avoid) to get `{ runId }`, then `POST`s `/api/ai/design/token` with `{ runId }` to get `{ token }` (the spec describes this as one call returning `{ runId, publicToken }`; the real backend from spec 20 splits trigger and token issuance into two routes, so the frontend performs both requests in sequence instead of inventing a merged endpoint). The returned `runId`/`token` feed `useRealtimeRun<typeof designAgent>(runId, { accessToken: token, enabled: ... })` from `@trigger.dev/react-hooks` (the task's exported type is imported with `import type` so no server-only code from `trigger/design-agent.ts` — Liveblocks node client, Gemini/`ai-sdk` calls — reaches the client bundle). `isRunActive` is derived straight from `run.status` against a local `ACTIVE_RUN_STATUSES` set (`PENDING_VERSION`/`QUEUED`/`DEQUEUED`/`EXECUTING`/`WAITING`/`DELAYED`; anything else — `COMPLETED` or any failure/cancel status — counts as terminal) rather than resetting `runId`/`publicToken` state from inside the status-watching `useEffect`, which ESLint's `react-hooks/set-state-in-effect` rule flags as a cascading-render anti-pattern; a `handledRunIdRef` guards the one-time "push a final message" side effect per run id instead. `AIArchitectTab` now combines this with the existing presence-derived `isAIThinking` (other participants' runs) into `isBusy`, which disables the textarea, starter chips, and send button (spinner icon) exactly as it already did for `isAIThinking` alone — so a run started by any participant disables input for everyone, and a run started by the local user also disables it immediately (before presence would reflect it). On submit, the existing chat-feed `sendMessage` first posts the user's prompt to `ai-chat` (spec 23's existing path, unchanged) and then calls `submitPrompt` to trigger the run; a start failure or a non-`COMPLETED` terminal run status posts a Ghost-AI-sender assistant message onto `ai-chat` (`isChatFeedMessageData`/the existing bubble styling renders it identically to a real reply) — satisfying "show errors as messages in the ai-chat feed" without a separate error-banner UI. `components/editor/ai-sidebar.tsx`'s `AISidebar`/`AIArchitectTab` signatures gained a `roomId: string` prop (needed to call the design routes, since the sidebar previously had no explicit knowledge of the room/project id — it resolved everything else from Liveblocks room context); `components/editor/workspace-shell.tsx` passes its existing `roomId` prop through. Canvas updates, the status strip (`ai-status-feed`), and the AI presence cursor/avatar are all unchanged and untouched — they continue to update automatically via Liveblocks per the existing spec 17/21/22 wiring; this spec only adds the missing button-to-backend plumbing. `npm run build` and `npm run lint` pass (lint initially flagged the first draft's `setRunId(undefined)`/`setPublicToken(undefined)` calls inside the completion effect via `react-hooks/set-state-in-effect`; fixed by the `ACTIVE_RUN_STATUSES`-on-`run.status` + `handledRunIdRef` approach above — same 2 pre-existing/unrelated warnings in `lib/liveblocks.ts`/`lib/prisma.ts` remain).

- Feature spec 25: Spec generation flow (backend only) — `trigger/generate-spec.ts` exports a `generateSpec` `schemaTask` (id `"generate-spec"`) validated with a Zod payload schema (`projectId`, `roomId`, `chatHistory`, `nodes`, `edges`); reused rather than duplicated: `chatFeedMessageSchema` from `types/tasks.ts` (exported for the first time — it was a private `const` backing spec 23's `isChatFeedMessageData` guard, unchanged in shape) validates chat history entries, and locally-defined `canvasNodeSchema`/`canvasEdgeSchema` (same `.loose()` — the current, non-deprecated equivalent of Zod v3's `.passthrough()` in this project's Zod v4 — pattern used so extra React Flow node/edge fields like `width`/`selected`/`measured` don't fail validation) constrain `data.shape` to `NODE_SHAPES`, mirroring `design-agent.ts`'s existing `ALLOWED_SHAPES` derivation instead of hardcoding the shape list again. The task builds a single Markdown-generation prompt (canvas nodes/edges rendered as a labeled list, plus the chat transcript for intent/context) and calls `generateText` (not `generateObject` — the spec calls for plain Markdown output, not structured data) against `gemini-2.5-flash` via `createGoogleGenerativeAI({ apiKey: process.env.GOOGLE_GENERATIVE_AI_API_KEY })`, the same explicit-key construction spec 21 already established for `design-agent.ts`. Realtime tracking uses Trigger.dev's own run **metadata** (`metadata.set("status", ...)` — `"start"` → `"processing"` → `"complete"`/`"error"`) rather than the Liveblocks presence/broadcast/feed mechanisms `design-agent.ts` uses, per the spec's literal "updates run metadata/status for realtime tracking" wording; no Liveblocks client is touched by this task at all. The task's return value is the raw Markdown string itself (not wrapped in an object) per the spec's "keep the task output as plain Markdown" instruction. `app/api/ai/spec/route.ts` (`POST`): requires `getCurrentUserIdentity()` (401); validates the body with a Zod schema built from the task's own exported `canvasNodeSchema`/`canvasEdgeSchema` plus `chatFeedMessageSchema` (`roomId`, `chatHistory`, `nodes`, `edges` — deliberately no `projectId` field in the accepted shape at all, unlike spec 20's `/api/ai/design` route, since this spec explicitly says not to trust a client-supplied project id); resolves project access by treating the validated `roomId` as the project id via the existing `getProjectWithAccess()` (same as every other mutation route), and only the server-resolved `project.id` — never anything from the request body — is passed to `generateSpec.trigger()` and written onto the new `TaskRun` row. `app/api/ai/spec/token/route.ts` (`POST`) mirrors `/api/ai/design/token`'s existing ownership-check pattern (401 if unauthenticated, look up `TaskRun` by `runId`, 403 unless `taskRun.userId` matches) but adds `expirationTime: "1h"` to the `auth.createPublicToken()` call, satisfying this spec's explicit 1-hour expiry requirement — a stricter behavior than spec 20's design-token route, which set no expiration at all (left untouched; out of this spec's scope). Scope limits honored: no frontend/spec-editor code touched, the generated Markdown is only ever returned as task output — nothing persists it to Blob or Prisma in this unit, and no new AI provider abstraction was introduced (same `@ai-sdk/google` + `ai` calls, called the same way as the existing design-agent task). `npm run build` passes (all 4 `/api/ai/*` routes listed in the route table); `npx tsc --noEmit` passes; targeted `npx eslint` on the 4 changed/added files (`trigger/generate-spec.ts`, `app/api/ai/spec/route.ts`, `app/api/ai/spec/token/route.ts`, `types/tasks.ts`) reports no issues — the full-repo `npm run lint` currently reports unrelated errors/warnings entirely inside `.trigger/tmp/build-*` (gitignored, ephemeral bundles regenerated by a running `trigger dev` dev-server process on every file save; not covered by `eslint.config.mjs`'s ignore list), pre-existing and unrelated to this change.

- Feature spec 26: Spec persistence + download (backend only) — new `prisma/models/project-spec.prisma` defines `ProjectSpec` (`id`, `projectId` FK cascade-delete to `Project`, `filePath`, `createdAt`, index on `[projectId, createdAt]`), and `project.prisma` gains the inverse `specs ProjectSpec[]` relation (same pattern as the existing `collaborators` relation); migration `20260707222340_add_project_spec` created and applied (`DATABASE_URL` reachable this session), `npx prisma generate` re-run to add `prisma.projectSpec` to the generated client. `trigger/generate-spec.ts` (the task built in spec 25, which previously only returned the generated Markdown as task output with no persistence) now persists on success, following the exact blob-then-Prisma-URL pattern the canvas route (`app/api/projects/[projectId]/canvas/route.ts`) already established: generates a `specId` via `node:crypto`'s `randomUUID()` (needed so the same id can name the blob path *and* be given explicitly to `prisma.projectSpec.create()` — Prisma's `@default(cuid())` only applies when `id` is omitted, and the spec's documented path shape `specs/{projectId}/{specId}.md` from `architecture-context.md` requires knowing the id up front, before the row exists), uploads the Markdown via `@vercel/blob`'s `put("specs/{projectId}/{specId}.md", text, { access: "private", addRandomSuffix: false, allowOverwrite: true, contentType: "text/markdown" })` (same `access: "private"` + fixed/overwritable pathname choice as the canvas route), then creates the `ProjectSpec` row with that blob URL as `filePath`; the task's return value changed from a bare Markdown string to `{ specId, markdown: text }` since nothing consumed the old bare-string return yet (spec 25's own notes confirmed no frontend/subscriber exists) and a future caller needs `specId` to hit the new download route. Persistence was placed inside the task itself rather than split into a separate route — consistent with `design-agent.ts`'s existing precedent of a Trigger.dev task owning its own full side-effect (there, direct Liveblocks Storage mutation; here, Blob+Prisma writes) rather than a request handler doing post-processing after the fact, and consistent with the architecture invariant that request handlers stay thin while background tasks own long-running/generation-adjacent work. New `app/api/projects/[projectId]/specs/[specId]/download/route.ts` (`GET`): requires `getCurrentUserIdentity()` (401) and `getProjectWithAccess(projectId, identity)` (403) — the same ownership-check pattern as every other project-scoped route; looks up the `ProjectSpec` by `specId` and 404s if it doesn't exist *or* its `projectId` doesn't match the URL's `projectId` (prevents a valid spec id from one project being fetched through another project's URL); fetches the private blob via `@vercel/blob`'s `get(spec.filePath, { access: "private" })` — a different retrieval path than the canvas route's plain `fetch(url)` (left untouched per this spec's "do not modify existing canvas persistence" scope limit), chosen here because `get()` is the SDK's documented, token-authenticated way to read a `private`-access blob, and because streaming the file content back through this route (rather than ever returning `spec.filePath` itself to the client) is what satisfies the spec's "do not expose Blob URLs without access checks" limit; 404s again if the blob fetch didn't return a `200`. Response is the raw Markdown streamed back as `text/markdown; charset=utf-8` with `Content-Disposition: attachment; filename="{specId}.md"` so browsers treat it as a file download. Scope limits honored: no frontend/UI code touched, no spec content stored in Prisma (only the blob `filePath` reference), the existing canvas persistence route was not modified. `npx tsc --noEmit`, `npm run build` (11 routes listed including the new download route), and a targeted `npx eslint` on both changed/added files all pass with no issues.

- Feature spec 27: Spec UI integration — added the one piece of backend surface the spec's UI needs but that didn't exist yet: `GET /api/projects/[projectId]/specs/route.ts` lists `ProjectSpec` rows (`{ id, createdAt }` only, ordered `createdAt desc`) behind the same `getCurrentUserIdentity()`/`getProjectWithAccess()` pattern as every other project-scoped route — a minimal, thin, single-responsibility addition rather than the "no backend logic" scope limit being violated, since the spec's own check-when-done criteria ("spec list loads for the current project") is unmeetable without it and no such route existed from specs 25/26. Deliberately does not select/return `filePath` — the client only ever needs a stable per-spec "filename," which is derived as `` `${specId}.md` `` (matching the existing download route's own `Content-Disposition` filename), so the Blob path reference never needs to leave the server at all, going further than spec 26's own "don't expose Blob URLs" bar. New `hooks/use-project-specs.ts` (`useProjectSpecs(projectId)`): fetches the list once per `projectId` and returns `{ specs, loading, error }`; `getSpecFilename`/`getSpecDownloadUrl`/`triggerSpecDownload` are also exported from here as the shared filename/URL/download-trigger utilities used by both the list and the preview modal. `triggerSpecDownload` creates a throwaway `<a href={downloadUrl} download>` and clicks it rather than fetching the file into JS memory — same-origin browser navigation carries the existing Clerk session cookie automatically, and the existing download route's `Content-Disposition: attachment` header is what actually drives the save-as behavior, so this satisfies "let the browser handle the file download" with no fetch/blob-URL plumbing on the client at all. New `components/editor/spec-preview-dialog.tsx` (`SpecPreviewDialog`): fetches spec content by calling the *same* existing download route via `fetch(...).then(r => r.text())` rather than adding a second "get content" endpoint — reusing it is safe because the `Content-Disposition` header only affects browser-initiated navigation, not a same-origin `fetch()` read, so the existing route doubles as both the modal's content source and the direct-download target without any backend change, and satisfies the spec's "do not access Blob directly from the client" / "fetch content through an existing endpoint" notes literally. Renders content via the newly-added `react-markdown` dependency (no Markdown renderer previously existed in this project) with a small `components` map translating headings/paragraphs/lists/code/links to this project's existing `text-copy-primary`/`text-copy-secondary`/`bg-subtle` tokens (no `@tailwindcss/typography` plugin was added, to avoid a second styling system — the spec's "use existing colors and tokens" note ruled out relying on a generic `prose` class anyway, which wouldn't touch this project's tokens). State design deliberately avoids ever calling a state setter synchronously inside a `useEffect` body (this repo's `react-hooks/set-state-in-effect` lint rule, and the working precedent already established in `canvas.tsx`'s load-on-mount effect, require every state update to happen inside a `.then()`/`.catch()`/`.finally()` continuation, never as a bare statement in the effect's synchronous top level): `useProjectSpecs` only calls `fetch(...)` synchronously in the effect and defers `setSpecs`/`setError`/`setLoading` into the promise chain; `SpecPreviewDialog` stores the latest fetch result as `{ specId, content, error }` and derives what to render (`isCurrent = loaded?.specId === specId`) at render time instead of eagerly clearing state when `specId` becomes `null` or changes — avoids a synchronous "reset" branch entirely while still never flashing a previous spec's stale content for a different `specId`. `components/editor/ai-sidebar.tsx`'s `SpecsTab` (previously spec 18/26's static hardcoded demo card) now takes a `roomId` prop, renders `useProjectSpecs(roomId)`'s list as clickable `SpecListItem`s (filename + formatted `createdAt`, each with its own Download button) with loading/error/empty states, and renders `<SpecPreviewDialog>` keyed off a local `previewSpecId` state set by clicking a list item; the "Generate Spec" button stays a no-op exactly as before — wiring it to `/api/ai/spec` (spec 25/26's existing trigger route) is explicitly out of this spec's scope and remains a future unit. Content is never persisted beyond the open dialog/list (no new global state, per the scope limit) — closing/switching specs simply lets the last-fetched result go stale and be overwritten by the next one. `npx tsc --noEmit`, `npm run build` (13 routes now listed, including the new list route), and a targeted `npx eslint` on all 4 changed/added files all pass with no errors.

- Fix (user-reported): clicking "Generate Spec" in the AI sidebar did nothing — no Trigger.dev run, no network request — because spec 27 deliberately left the button a no-op (wiring it to the spec-generation backend was explicitly out of that spec's scope). Wired it up following the exact same two-request-handshake pattern `useDesignAgentRun` already established for design generation: new `useSpecGenerationRun(roomId, onComplete)` in `components/editor/ai-sidebar.tsx` `POST`s `/api/ai/spec` with `{ roomId, chatHistory, nodes, edges }` → `POST`s `/api/ai/spec/token` with `{ runId }` → subscribes via `useRealtimeRun<typeof generateSpec>` (task type imported with `import type` so no server-only Gemini/Blob/Prisma code from `trigger/generate-spec.ts` reaches the client bundle, same reasoning as the existing `designAgent` type import); on a terminal run status it posts a Ghost-AI-sender message onto the existing `ai-chat` feed (success or failure) via the same `postAssistantMessage` pattern as `useDesignAgentRun`, and calls `onComplete()` only on `"COMPLETED"`. `chatHistory` is sourced from the already-existing `useChatFeed()` hook (stripped of its `id` field, which the backend's `chatFeedMessageSchema` would silently strip anyway since Zod's default object mode is "strip", not "strict" — done client-side anyway for a clean payload); `nodes`/`edges` are read via `useLiveblocksFlow<CanvasNode, CanvasEdge>()` called a second time in `SpecsTab` (safe — the hook is just `useStorage`/`useMutation`/`useHistory` under the hood, no singleton state, and `AISidebar` already sits inside the same `RoomProvider` as the canvas per spec 22's notes) — its own `onNodesChange`/`onEdgesChange`/etc. mutators are ignored since `SpecsTab` only needs a read-only snapshot, and its `isLoading` flag (true until Storage's initial sync) disables the Generate button so a click can't fire with a stale empty canvas before the room has actually loaded. `useProjectSpecs` gained a `refresh()` function (a `useCallback`, deliberately *not* called from the hook's own load-on-mount effect — keeping it a plain function called only from event/callback code is what keeps it clear of the `react-hooks/set-state-in-effect` rule the mount effect itself has to obey) that `SpecsTab` passes to `useSpecGenerationRun` as `onComplete`, so the spec list refetches automatically right when a generation finishes — no manual reload needed. `npx tsc --noEmit`, targeted `npx eslint` on both changed files, and `npm run build` all pass with no errors. Requires `GOOGLE_GENERATIVE_AI_API_KEY` and `TRIGGER_SECRET_KEY` to be set for the run to actually execute (confirmed both are present in `.env.local`, values not inspected). Not verified against a live Trigger.dev run in this session (no browser/session available here) — ask the user to confirm: clicking "Generate Spec" shows a spinner on the button, a run appears in the `trigger dev` CLI output / dashboard, a Ghost AI message appears in the chat feed on completion or failure, and the new spec shows up in the list without a manual refresh.

- Fix (user-reported): clicking a project name in the left sidebar (`ProjectSidebar`) did nothing — the only way to open an existing project was to create a new one. Root cause — `project-sidebar.tsx`'s `ProjectListItem` rendered the project name as a plain `<span>` with no click handler or link at all; only the hover-revealed rename/delete icon buttons were wired up. Fixed by wrapping the name in a Next.js `<Link href={`/editor/${project.id}`}>` (route confirmed via `app/editor/[roomId]/page.tsx` — project id and room id are the same value throughout this codebase, per spec 19's notes); the rename/delete buttons already call `e.stopPropagation()` so they still work without triggering navigation. Added an `onNavigate` prop (wired to the sidebar's existing `onClose`) so clicking a project also closes the mobile overlay sidebar, matching the existing backdrop-click-to-close behavior. Also fixed a related bug found in the same file's data flow: `hooks/use-project-actions.ts`'s `handleDelete` read `params?.projectId` to detect whether the project being deleted was the currently-open one, but the actual dynamic route segment is named `roomId` (`app/editor/[roomId]/page.tsx`), so that check silently always evaluated to `null`/false — deleting the active project would `router.refresh()` instead of redirecting to `/editor`, leaving the user on a now-deleted project's page. Changed to `params?.roomId`. `npx tsc --noEmit` passes. Not verified interactively in a browser (no Clerk session/browser automation in this environment) — ask the user to confirm: clicking any project name in the sidebar navigates to that project's canvas, clicking rename/delete icons still does not navigate, and deleting the currently-open project now redirects to `/editor` instead of just refreshing.

## In Progress

- None.

## Next Up

- Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session for feature spec 19 (canvas autosave): editing the canvas (add/move/delete a node, draw an edge) shows "Saving…" then settles on "Saved" ~1.5s after the last change, without firing on every keystroke; reloading the page for a project with saved content loads the saved nodes/edges back onto the canvas; opening the same project in a second tab/session while the first still has live content does NOT get overwritten by a load (room-not-empty skip); killing the network (or an invalid/missing `BLOB_READ_WRITE_TOKEN`) shows "Save failed" in red; a brand-new project with an empty canvas shows no false "Save failed" on first load.
- Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session: the AI sidebar slides in smoothly from the right without pushing/resizing the canvas, the header/tabs/empty-state/starter-chip styling and colors read as intended, sending a message shows both the user (cyan/brand) and assistant (AI-accent) message stylings, and the textarea grows between 72px and 160px as text is typed and stops growing beyond that, `Enter` sends while `Shift+Enter` inserts a newline. (The Specs tab's demo card mentioned in the original spec 18 note no longer exists — spec 27 replaced it with the real spec list/preview/download UI; see the spec 27 verification bullet below.)
- Move to the next feature spec in `context/feature-specs/`.
- Manually verify feature spec 17 in a real browser session with two signed-in sessions: the collaborator avatar group appears top-right of the canvas only (never on the editor home navbar), shows profile photos or initials, overlaps up to 5 with a `+N` chip beyond that, shows the divider only when a collaborator is present, and the Clerk `UserButton` renders at the same size as the collaborator avatars; live cursors appear for other participants only (never your own), track pan/zoom correctly, and their pointer/badge color matches each participant's assigned presence color.
- Manually verify in a real browser session: each of the 6 shapes renders correctly on the canvas (not just rectangles), selected nodes show a brighter border, dragging a shape from the panel shows a ghost preview matching that shape/size attached to the cursor that disappears on drop or cancel, hovering a node reveals its 4 connection handles which can be dragged to another node to create an edge, selected nodes show resize handles that enforce the minimum size and persist after reselecting, double-click label editing (live typing, blur-to-commit, Escape-to-cancel, no drag/pan leakage) behaves as specified, and selecting a node shows the floating color toolbar just above it (swatch click updates both fill and text color immediately, active swatch is visually distinct, hover shows a tight glow, and clicking a swatch never drags the node or pans/deselects the canvas).
- Manually verify feature spec 16 (control bar + keyboard shortcuts) per the checklist noted under that spec above.
- Set `LIVEBLOCKS_SECRET_KEY` in `.env.local` (and deployment env) — the canvas wrapper is wired up but the auth route is unauthenticated against the real Liveblocks API until this is configured, so live multi-user sync/cursors can't be verified locally yet.
- Feature spec 24 wired the client trigger point that spec 21's note above flagged as missing — submitting a prompt in the AI sidebar now actually calls `POST /api/ai/design` → `POST /api/ai/design/token` → subscribes via `useRealtimeRun`. Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session: submitting a prompt disables the textarea/starter-chips/send button (spinner icon) for every connected participant until the run finishes; a Ghost AI cursor (color `#6457f9`) and collaborator avatar appear to all connected participants while the run is active and disappear a couple seconds after; nodes/edges appear on the canvas honoring the 6 allowed shapes and 8 allowed colors; a final Ghost AI message appears in the `ai-chat` feed after completion; a failed/canceled run still re-enables input and posts an error-flavored Ghost AI message instead of hanging disabled forever; re-prompting in the same room can move/resize/recolor/delete nodes it (or a user) already created, referencing them by the ids visible in the room.
- `trigger/design-agent.ts` still publishes status via the old `broadcastEvent`/`RoomEvent` (`{ type: "ai-status", runId, status, message }`) from spec 21, which the AI sidebar's status strip (spec 22) does not read from — the sidebar subscribes to the Liveblocks **Feed** named `ai-status-feed` (`useFeedMessages`) instead, per spec 22's explicit "reuse a Liveblocks feed" instruction. This mismatch is now more visible since spec 24 wired the real trigger point: the status strip will stay empty/idle-looking through an actual run until a future unit switches `design-agent.ts`'s `publishStatus` from `client.broadcastEvent` to `client.createFeedMessage({ roomId, feedId: "ai-status-feed", data: { text: message } })` (from `@liveblocks/node`) — left untouched by spec 24 since it's Trigger.dev task logic, out of that spec's frontend-only scope. `broadcastEvent`/`RoomEvent` can likely be removed at that point if nothing else consumes it.
- Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session for feature spec 22: opening the AI sidebar shows no status banner and an enabled input when idle; once `trigger/design-agent.ts` is wired to publish onto `ai-status-feed` (see bullet above), triggering a run should show the spinner + latest status text banner, a disabled textarea/send-button/starter-chips (send button shows a spinner icon) for every connected participant, and the Ghost AI cursor's name badge should show a small spinner next to its name while `thinking` is true.
- Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session for feature spec 23 (sidebar chat feed): opening a room with no chat history yet shows the empty state and starter chips; sending a message (via the input or a starter chip) appears immediately in the sender's own session right-aligned in brand styling and clears the input; a second participant in the same room sees the same message left-aligned with the sender's name and a readable timestamp, in real time with no refresh; killing connectivity (or otherwise forcing `createFeedMessage` to reject) shows the "Couldn't send your message. Try again." error text under the input without clearing the draft; sending while a design-agent run is active (`isAIThinking`) is blocked the same way the old placeholder chat was.
- `types/tasks.ts`'s `RoomFeedMessageData` (the `FeedMessageData` Liveblocks override) is a workaround for a pre-existing gap where named interfaces don't satisfy Liveblocks' internal `Json` constraint check in the installed `@liveblocks/core` version — worth an upstream Liveblocks issue/support check at some point, since every future feed will need its fields added to this one shared envelope type rather than getting its own clean type.
- Feature spec 25 (spec generation flow) is backend-only per its own scope limits: `POST /api/ai/spec` → `POST /api/ai/spec/token` → `generateSpec` Trigger.dev task exist. Feature spec 26 closed the persistence gap, feature spec 27 closed the display gap (spec list, Markdown preview, download), and the "Generate Spec" no-op flagged here was wired up in the fix noted directly above (real trigger + realtime subscription + auto-refresh on completion). What's still open: the run only reports coarse start/terminal status via `postAssistantMessage`/`onComplete` — it doesn't surface `generateSpec`'s own `metadata.set("status", ...)` progress ticks (`"start"`/`"processing"`) the way the design-agent flow's `ai-status-feed` banner does, so there's no live "generating…" indicator in the Specs tab while a run is in flight beyond the button's spinner.
- Pre-existing lint config gap noticed while verifying feature spec 25: `.trigger/tmp/build-*` (ephemeral bundles a running `trigger dev` dev-server regenerates on every file save under `trigger/`) is gitignored but not excluded by `eslint.config.mjs`'s `globalIgnores`, so a full-repo `npm run lint` while `trigger dev` is running surfaces hundreds of errors/warnings from vendored/bundled code that isn't part of this project's source — worth adding `.trigger/**` to `eslint.config.mjs`'s ignore list in a future pass; worked around this session by linting only the changed files directly.
- Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session for feature spec 27 (spec UI integration): opening the Specs tab with no specs yet shows the empty state; with specs present, each list item shows a formatted date and a `{specId}.md` filename and is clickable; clicking a list item opens a modal that shows a loading spinner then renders the spec's Markdown (headings/lists/code/links styled, not literal `#`/`*` characters); pressing Escape or the modal's close button closes it; clicking Download (list item or modal) triggers a real browser file download of the `.md` file; switching between two different specs' previews in a row never flashes the previous spec's content.

## Open Questions

- None yet.

## Architecture Decisions

- Dark-only theme: all shadcn component variables set in `:root` to dark values — no `.dark` class needed.
- CSS custom properties in globals.css serve two layers: shadcn variables (--background, --primary, etc.) and project tokens (--bg-base, --accent-primary, etc.).
- Tailwind @theme inline maps both layers to utility classes: bg-base, text-copy-primary, border-surface-border, text-brand, bg-brand-dim, etc.
- shadcn/ui components live in components/ui/ and must not be modified after generation.

## Session Notes

- Project uses Next.js 16.2.9, React 19, Tailwind v4, TypeScript strict mode.
- Path alias @/* maps to project root.
- VS Code shows @apply warnings in globals.css — these are CSS lint false positives, not build errors.
