# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 1: Foundation

## Current Goal

- Move to the next feature spec.

## Completed

- Feature spec 01: Design system — shadcn/ui initialized, all 7 UI primitives added (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), lucide-react installed, lib/utils.ts with cn() created, globals.css updated with full dark theme token set.
- Feature spec 02: Editor chrome — `components/editor/editor-navbar.tsx` (fixed h-12 navbar, sidebar toggle with PanelLeftOpen/PanelLeftClose, left/center/right sections, dark bg + bottom border) and `components/editor/project-sidebar.tsx` (floating overlay z-50, slides from left without pushing content, Projects header + close button, My Projects / Shared tabs with empty placeholders, full-width New Project button). Dialog pattern ready via existing shadcn Dialog component and globals.css tokens.
- Feature spec 03: Auth — `proxy.ts` at project root wires Clerk middleware (all routes protected except public sign-in/sign-up paths from env vars); `ClerkProvider` wraps root layout with `@clerk/ui` dark theme and CSS variable overrides (no hardcoded colors); `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` use two-panel layout (left: logo + tagline + feature list, hidden on small screens; right: Clerk form); root `/` redirects authenticated users to `/editor` and unauthenticated to `/sign-in`; `UserButton` added to editor navbar right section. `@clerk/ui` installed.
- Feature spec 04: Project dialogs — `hooks/use-project-dialogs.ts` manages dialog/form/loading state; `components/editor/project-dialogs-context.tsx` provides context to layout tree; `CreateProjectDialog` (name input + live slug preview), `RenameProjectDialog` (prefilled, auto-focus, Enter submits), `DeleteProjectDialog` (destructive confirm, no input) added to editor layout; sidebar updated with rename/delete actions on owned projects (hidden for shared), mobile backdrop scrim via `md:hidden` overlay; editor home screen (`app/editor/page.tsx`) shows heading, description, and New Project button wired to Create dialog. Mock project data only.
- Feature spec 05: Prisma data models — `prisma/models/project.prisma` defines `Project` (ownerId→Clerk, name, optional description, `DRAFT`/`ARCHIVED` status, `canvasJsonPath`, timestamps, indexes on ownerId+createdAt) and `ProjectCollaborator` (project FK cascade delete, email, createdAt, unique on projectId+email, indexes on email and projectId+date). `lib/prisma.ts` exports a cached singleton: branches on DATABASE_URL prefix (`prisma+postgres://` → `accelerateUrl`, otherwise `@prisma/adapter-pg`). Client generated to `app/generated/prisma`. Migration pending database connectivity.
- Feature spec 06: Project APIs — `app/api/projects/route.ts` (GET lists owner's projects ordered by createdAt desc; POST creates with defaulted name "Untitled Project"); `app/api/projects/[projectId]/route.ts` (PATCH renames, DELETE deletes). Both mutation routes enforce 401 for unauthenticated and 403 for non-owner. `npm run build` passes.
- Feature spec 07: Wire editor home — `lib/data/projects.ts` server-side helper fetches owned projects (by ownerId) and shared projects (by collaborator email via `currentUser()`); `app/editor/layout.tsx` converted to async server component, fetches project lists and passes to `EditorShell`; `components/editor/editor-shell.tsx` client shell holds sidebar toggle state and provides `ProjectDialogsContext`; `hooks/use-project-actions.ts` manages dialog state and mutations (create: slugify + unique suffix → roomId → POST /api/projects → navigate; rename: PATCH → refresh; delete: DELETE → redirect if active workspace else refresh); `POST /api/projects` updated to accept optional client-generated `id`; `app/editor/page.tsx` converted to server component using `NewProjectButton` client leaf; dialogs wired to real handlers with loading states. `npm run build` passes.
- Feature spec 08: Editor workspace shell — `lib/project-access.ts` provides `getCurrentUserIdentity()` (Clerk userId + primary email) and `getProjectWithAccess()` (checks owner or collaborator); `components/editor/access-denied.tsx` centered layout with lock icon, message, and link back to `/editor`; `app/editor/[roomId]/page.tsx` server component: unauthenticated → redirect `/sign-in`, missing/unauthorized project → `AccessDenied`, authorized → `WorkspaceShell`; `components/editor/workspace-shell.tsx` full-viewport overlay (`fixed inset-0 z-50`) with workspace navbar (project name, Share button, AI sidebar toggle, UserButton), `ProjectSidebar` with `activeProjectId` highlighting current room, canvas placeholder, and collapsible AI sidebar placeholder; `ProjectSidebar` updated with optional `activeProjectId` prop to highlight active project. `npm run build` passes.
- Feature spec 09: Share dialog — `lib/collaborators.ts` lists a project's `ProjectCollaborator` rows and enriches each by email via Clerk Backend API (`clerkClient().users.getUserList({ emailAddress })`), falling back to email-only when no Clerk user matches; `app/api/projects/[projectId]/collaborators/route.ts` (GET: owner-or-collaborator can list; POST: owner-only invite, validates email format, rejects inviting own email, 409 on duplicate via Prisma `P2002`); `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` (DELETE: owner-only remove, scoped to projectId). `hooks/use-share-collaborators.ts` owns dialog open state and all collaborator actions (`openDialog` fetches on open, `invite`, `remove`, `copyLink` with temporary `copied` flag); `components/editor/share-dialog.tsx` is a presentational dialog (owner sees invite input + remove buttons, collaborators see read-only list, avatar falls back to initials when no Clerk image). `app/editor/[roomId]/page.tsx` now computes `isOwner` (`project.ownerId === identity.userId`) and passes it to `WorkspaceShell`, which instantiates `useShareCollaborators` and wires the navbar Share button to `share.openDialog`. `npm run build` and `npm run lint` pass.
- Feature spec 10: Liveblocks setup — `liveblocks.config.ts` types `Presence` (`cursor: {x,y} | null`, `isThinking: boolean`) and `UserMeta.info` (`name`, `avatar`, `color`); unused `Storage`/`RoomEvent`/`ThreadMetadata`/`RoomInfo` placeholders changed from `{}` to `Record<string, never>` to satisfy `@typescript-eslint/no-empty-object-type`. `@liveblocks/node` installed. `lib/liveblocks.ts` exports `getLiveblocksClient()` — a globally cached singleton (same pattern as `lib/prisma.ts`), constructed lazily on first call (not at module load) so a missing `LIVEBLOCKS_SECRET_KEY` doesn't break `next build`'s page-data collection — and `getCursorColorForUser(userId)`, a deterministic hash-to-palette mapper (9-color fixed palette). `app/api/liveblocks-auth/route.ts`: requires Clerk auth via `getCurrentUserIdentity()` (401 if absent), reads `roomId` from the request body's `room` field (Liveblocks client posts project ID as room), verifies access via `getProjectWithAccess()` (403 if denied), calls `getOrCreateRoom(roomId, { defaultAccesses: [] })`, then issues an access-token session (`prepareSession` + `session.allow(roomId, session.FULL_ACCESS)`) carrying `userInfo: { name, avatar, color }` sourced from Clerk's `currentUser()` and `getCursorColorForUser`. Access-token model chosen (over ID tokens) because authorization is already fully resolved by our own DB-backed ownership/collaborator check — the token only needs to grant the single verified room, not rely on Liveblocks-side room permissions. `LIVEBLOCKS_SECRET_KEY` env var not yet set locally; route is unauthenticated against the real Liveblocks API until configured. `npm run build` and `npm run lint` pass.
- Feature spec 11: Base canvas — `types/canvas.ts` (`CanvasNodeShape` union of the 6 shapes, `CanvasNodeData` with `label`/`color`/`shape` — extends `Record<string, unknown>` so it satisfies `@xyflow/react`'s `Node<T>` data constraint, `CanvasNode = Node<CanvasNodeData, "canvasNode">`, `CanvasEdge = Edge<Record<string, never>, "canvasEdge">`) already existed from spec 10 groundwork and needed only the `Record<string, unknown>` fix. `components/editor/canvas.tsx` (client) calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ nodes: { initial: [] }, edges: { initial: [] }, suspense: true })` from `@liveblocks/react-flow` and renders `ReactFlow` with `connectionMode={ConnectionMode.Loose}`, `fitView`, dot-pattern `Background`, and `MiniMap`; imports `@xyflow/react/dist/style.css` directly in the component file (external stylesheets are importable anywhere under `app/` in this Next.js version, not just the root layout). `components/editor/canvas-room.tsx` (client) sets up the room: a local `CanvasErrorBoundary` class component (no `react-error-boundary` dependency — Suspense doesn't catch connection errors, so a boundary is needed) wraps `LiveblocksProvider` (`authEndpoint="/api/liveblocks-auth"`) → `RoomProvider` (`id={roomId}`, `initialPresence={{ cursor: null, isThinking: false }}` — `isThinking` is required by the `Presence` type from spec 10 even though the spec only calls out `cursor`) → `ClientSideSuspense` (simple "Loading canvas…" fallback) → `Canvas`. `components/editor/workspace-shell.tsx` now renders `<CanvasRoom roomId={roomId} />` in place of the "Canvas coming soon" placeholder; `<main>` changed to `relative flex-1` (no more centering) so the canvas fills the available space — `Canvas` wraps `ReactFlow` in a `h-full w-full` div since React Flow requires an ancestor with a definite height and the provider/suspense layers in between render no DOM. Scope intentionally excludes: `Controls`, custom node/edge rendering, persistence, AI behavior. `npm run build` and `npm run lint` pass.
- Feature spec 12: Shape panel — `types/canvas.ts` extended with the `NODE_COLORS`/`NODE_SHAPES` constants that `context/ui-context.md` already documented as living there: `NODE_COLORS` (8 named fill/text pairs, `DEFAULT_NODE_COLOR = NODE_COLORS.neutral`) and `NODE_SHAPES` (`NodeShapeDefinition[]` pairing each `CanvasNodeShape` with a Lucide icon — `RectangleHorizontal`, `Diamond`, `Circle`, `Pill`, `Cylinder`, `Hexagon` — and a default `{ width, height }`; rectangle 160×80, diamond 140×140, circle 100×100, pill 140×60, cylinder 100×120, hexagon 120×100). `components/editor/shape-panel.tsx` (client) renders a floating pill toolbar (`absolute inset-x-0 bottom-6`, rounded-full surface) with one draggable icon button per `NODE_SHAPES` entry; `onDragStart` writes `{ shape, width, height }` as JSON under a custom `application/x-canvas-shape` MIME type (exported as `SHAPE_DRAG_MIME`, payload typed as `ShapeDragPayload`). `components/editor/canvas.tsx` restructured: `Canvas` now wraps a new inner `CanvasFlow` component in `<ReactFlowProvider>` (required because `useReactFlow()` — needed for `screenToFlowPosition` — only works in a descendant of the provider, not in the same component that renders `<ReactFlow>`); the wrapper `div` around `<ReactFlow>` gets `onDragOver` (calls `preventDefault`, sets `dropEffect = "move"`) and `onDrop` handlers; on drop, the shape payload is read via `SHAPE_DRAG_MIME`, the pointer position is converted with `screenToFlowPosition({ x: clientX, y: clientY })`, a node ID is built as `` `${shape}-${Date.now()}-${container}` `` where `container` is a `useRef`-backed incrementing counter (guards against duplicate IDs from same-millisecond drops), and the new node (`label: ""`, `color: DEFAULT_NODE_COLOR.fill`, the dragged `shape`) is added via `onNodesChange([{ type: "add", item: newNode }])` — not `useReactFlow().setNodes`, since nodes are controlled by Liveblocks Storage and only `onNodesChange` (from `useLiveblocksFlow`) writes through to the synced room; `nodeTypes={{ canvasNode: CanvasNodeRenderer }}` registered as a module-level constant (avoids React Flow's re-creation warning). `npm run build` and `npm run lint` pass. Not verified interactively in a browser: no Clerk session/browser automation available in this environment, and a separate dev server was already running locally under the user's own session — ask the user to confirm the drag-drop-render loop visually in that session.
- Fix (`context/current-issues.md`): all dropped shapes were rendering as plain bordered rectangles regardless of the dragged shape. Root cause — `components/editor/canvas-node.tsx` from spec 12 was intentionally shape-agnostic (one bordered `div` for every shape) as a placeholder, and nothing had implemented the shape-specific visuals `context/ui-context.md` calls for. Added `getNodeTextColor(fill)` to `types/canvas.ts` (looks up the matching `NODE_COLORS` pair by fill, falls back to `DEFAULT_NODE_COLOR.text`). Rewrote `canvas-node.tsx` to switch on `data.shape`: `rectangle` keeps the bordered `div` (small radius); `circle`/`pill` both use a `rounded-full` div (stadium shape naturally falls out of `rounded-full` on a non-square box); `diamond`/`hexagon`/`cylinder` render as inline SVG per `ui-context.md` (`polygon` for diamond/hexagon, a body `path` + top `ellipse` for cylinder), each using `vectorEffect="non-scaling-stroke"` so the 1px border doesn't distort under the `preserveAspectRatio="none"` stretch, and stroke color sourced from `var(--border-default)` via inline `style` (CSS vars aren't reliably resolved in raw SVG presentation attributes, but are in `style`). Label is an absolutely-positioned centered `span` on top of the shape in every case, colored via `getNodeTextColor`. `npm run build` and `npm run lint` pass.
- Feature spec 13: Node shape rendering + drag preview — extracted the per-shape visual (rectangle/pill/circle as CSS `div`s, diamond/hexagon/cylinder as inline SVG) out of `canvas-node.tsx` into a new shared `components/editor/shape-visual.tsx` (`ShapeVisual({ shape, color, strokeColor })`), so the same shape-rendering logic can back both the live node and the drag preview instead of duplicating it. `canvas-node.tsx` now reads `selected` from `NodeProps` and passes `strokeColor: selected ? "var(--accent-primary)" : "var(--border-default)"` into `ShapeVisual` — borders stay subtle at rest and brighten to the brand cyan when a node is selected. SVG shapes already scaled with node size via `preserveAspectRatio="none"` + `h-full w-full` (unchanged, carried over from the earlier fix). Drag preview: `components/editor/shape-panel.tsx` renders one hidden `ShapeVisual` per `NODE_SHAPES` entry, sized to that shape's `defaultSize`, inside a `fixed left-[-9999px] top-[-9999px]` container (off-screen but still painted — `display:none`/`opacity:0` elements don't work as native drag images in Chromium) and keeps refs to each in a `Partial<Record<CanvasNodeShape, HTMLDivElement | null>>`. `onDragStart` calls the native `event.dataTransfer.setDragImage(preview, width/2, height/2)` so the browser's built-in drag-image renderer shows the correct shape/size centered on the cursor and automatically follows the pointer and disappears on drop or cancel — no custom mousemove tracking or extra state needed. Node creation on drop, the shape panel layout, and resize/label editing were left untouched per scope limits. `npm run build` and `npm run lint` pass.
- Fix (`context/current-issues.md`): dropped shapes couldn't be connected to each other. Root cause — `canvas-node.tsx`'s custom `canvasNode` type never rendered any `Handle` components; React Flow (even with `onConnect` wired and `connectionMode={ConnectionMode.Loose}` set on `<ReactFlow>`) has nothing to start or end a connection drag on without them. Added four `Handle`s (`Position.Top/Right/Bottom/Left`, `type="source"`, ids matching position — `isConnectableStart`/`isConnectableEnd` default `true` so each works bidirectionally under Loose mode) to `CanvasNodeRenderer`, styled per `context/ui-context.md` (white, `8px`, subtle border) and hidden at rest / revealed on node hover via a `group`/`group-hover:opacity-100` class on the wrapping `div` (inline `style` used for handle color/size so it reliably overrides `@xyflow/react`'s default dark handle color, which a same-specificity Tailwind class can't guarantee given import order). `npm run build` and `npm run lint` pass. `context/current-issues.md` cleared.
- Feature spec 14: Node resizing + inline label editing — `types/canvas.ts` adds `MIN_NODE_SIZE` (`{ width: 48, height: 40 }`) next to `CanvasNodeSize`. `canvas-node.tsx`: added `<NodeResizer isVisible={selected} minWidth minHeight handleStyle lineStyle />` (from `@xyflow/react`) inside `CanvasNodeRenderer` — resize handles are cyan-on-dark (`var(--accent-primary)` border on a `var(--bg-elevated)` fill) to read as subtle but on-brand, and only show when the node is selected; dimension changes flow through `NodeResizer` → React Flow's internal `triggerNodeChanges` → the same `onNodesChange` from `useLiveblocksFlow` already wired in `canvas.tsx`, which `@liveblocks/react-flow` handles via its built-in `"dimensions"` case (writes synced top-level `width`/`height` onto the storage node) — no new sync code needed. Inline label editing: double-clicking the node body swaps the centered `NodeLabel` span for a `LabelEditor` textarea absolutely positioned in the same `inset-0` box (no layout shift); the textarea carries `nodrag nopan` classes (React Flow's default drag/pan-suppression classes) so typing, selecting text, or dragging the cursor inside it never moves the node or pans the canvas. Both the label span and the textarea show a muted "Label" placeholder (`var(--text-faint)`) when `data.label` is empty. Because nodes are Liveblocks-storage-controlled (not React Flow's own `setNodes`/`updateNodeData`, which only mutate an internal batch that a controlled `nodes` prop immediately overwrites), label commits are dispatched as `onNodesChange([{ id, type: "replace", item: { ...node, data: { ...node.data, label } } }])` from a new `updateLabel` callback defined in `canvas.tsx` (has access to the full canonical node from `useLiveblocksFlow`'s `nodes` array, required because `NodeReplaceChange` reconciles with `extent: "full"` — omitting any existing top-level key like `style`/`width`/`height` would delete it); passed down to `CanvasNodeRenderer` via a new `CanvasNodeActionsContext`/`CanvasNodeActionsProvider` (plain React context, not node `data`, since `data` is synced to storage and can't carry a function) that `canvas.tsx` provides around `<ReactFlow>`. Every keystroke calls `updateLabel` immediately (live-syncs to collaborators as you type, per spec), while a local `draftLabel` state keeps typing responsive independent of the Storage round-trip; blur commits (stops editing, keeps the last-typed value); `Escape` reverts to the label captured when editing started and stops editing. Shape rendering, the shape panel, and drop-to-create were untouched per scope limits.
- Fix: double-click-to-edit did nothing (couldn't select or type into the label at all). Root cause — React Flow's `<ReactFlow>` defaults `zoomOnDoubleClick` to `true`, wiring `d3-zoom`'s own `dblclick.zoom` listener directly onto the pane DOM element, which sits *below* React's delegated synthetic-event listener in the DOM tree. `d3-zoom`'s handler calls `event.stopImmediatePropagation()` on the native event on every double-click that isn't inside a `.nopan`-classed element, which fired before the event could ever bubble up to reach our `onDoubleClick` on the node — so `startEditing` never ran and the textarea never mounted. Fixed by adding `zoomOnDoubleClick={false}` to `<ReactFlow>` in `canvas.tsx` (double-click-to-zoom wasn't relied on anywhere else). Also fixed a real but secondary issue found in the same pass: `@xyflow/react`'s stylesheet sets `user-select: none` on every `.react-flow__node`, which is inherited by the textarea and blocks mouse-drag text selection inside it (typing/focus are unaffected by `user-select`, only highlighting) — fixed by adding `select-text` plus an explicit inline `userSelect: "text"` directly on the `<textarea>` in `canvas-node.tsx` (a direct rule on the element itself always wins over an inherited value, regardless of the ancestor rule's specificity or stylesheet order). `npm run build` and `npm run lint` pass.
- Not verified interactively in a browser (no Clerk session/browser automation in this environment, and a separate dev server was already running locally under the user's own session, per the same caveat as spec 12) — ask the user to confirm in that session: selecting a node shows corner/edge resize handles that respect the minimum size, resizing persists after deselecting/reselecting, double-clicking a node's label opens a smoothly-overlaid textarea (no jump) that can be typed into and text-selected with the mouse, typing updates the label live, blur/Escape close editing as expected (Escape discards the in-progress edit), dragging/panning don't trigger while typing in the textarea, and double-clicking elsewhere on the canvas no longer zooms in.
- Feature spec 16: Canvas ergonomics — control bar, keyboard shortcuts — new `hooks/use-keyboard-shortcuts.ts` (`useKeyboardShortcuts({ reactFlowInstance, undo, redo })`) attaches a single `window` `keydown` listener: ignores events when `event.target` is an `<input>`/`<textarea>`/`isContentEditable` element; `Cmd/Ctrl+Z` undoes (or redoes with Shift held), `Cmd/Ctrl+Y` redoes, `+`/`=` zoom in, `-` zooms out (all `preventDefault`ed so the browser's own zoom/undo doesn't fire); zoom keys call `reactFlowInstance.zoomIn`/`zoomOut` with a `duration` so keyboard zoom animates the same as the button. New `components/editor/canvas-control-bar.tsx` (`CanvasControlBar`) renders a pill toolbar (same visual pattern as `shape-panel.tsx`: `rounded-full border-surface-border bg-surface` pill, circular icon buttons) positioned `absolute bottom-6 left-6 z-20` — bottom-left, and stacked above the shape panel's `z-10` per the spec's "sit above the shape panel" (the shape panel itself stays bottom-center per the spec's own "don't change the shape panel" limit, so the two pills sit side by side without overlapping); zoom group (`ZoomOut`/`Maximize`/`ZoomIn` from lucide) calls `reactFlowInstance.zoomOut`/`fitView`/`zoomIn` directly, each with the same animation `duration`; a `bg-surface-border` divider (1px) separates it from the history group (`Undo2`/`Redo2`), which is disabled (`disabled:opacity-40 disabled:pointer-events-none`) when `canUndo`/`canRedo` is false. `canvas.tsx`: `CanvasFlow` now calls Liveblocks' room-scoped `useUndo`/`useRedo`/`useCanUndo`/`useCanRedo` from `@liveblocks/react` directly (this project's `liveblocks.config.ts` only declares global `Liveblocks` types, not a `createRoomContext` — these hooks resolve the room from React context at call time, same as the existing `useLiveblocksFlow` pattern already in this file, so no new context/provider was needed); `useReactFlow()`'s return value is now held in a `reactFlowInstance` variable (previously destructured immediately to only `screenToFlowPosition`) so the same instance can be passed to both `useKeyboardShortcuts` and `<CanvasControlBar>`; `<MiniMap />` removed from inside `<ReactFlow>` per the spec's step 6. Scope limits honored: shape panel, node/edge rendering, and the Liveblocks room/provider setup in `canvas-room.tsx` were left untouched; no extra canvas controls were added beyond the zoom/history pairs and their shortcuts. `npm run build` and `npm run lint` pass (lint's 2 warnings are pre-existing/unrelated, in `lib/liveblocks.ts` and `lib/prisma.ts`).
- Not verified interactively in a browser (same environment caveat as prior specs — no Clerk session/browser automation available here) — ask the user to confirm in their own session: the control bar appears bottom-left without overlapping the shape panel, zoom in/out/fit view animate smoothly and match the `+`/`-` keyboard shortcuts, undo/redo buttons visibly dim and stop responding to clicks when there's nothing to undo/redo, `Cmd/Ctrl+Z`/`Shift+Z`/`Y` drive the same Liveblocks history as the buttons, and none of the shortcuts fire while typing in a node's label textarea (`use-keyboard-shortcuts.ts`'s editable-target check).
- Feature spec 15: Node color toolbar — the predefined color palette (`NODE_COLORS`, 8 fill/text pairs) and text-color lookup (`getNodeTextColor`) already existed in `types/canvas.ts` from spec 12/the current-issues fix, so no new palette or type work was needed there. New `components/editor/node-color-toolbar.tsx` (`NodeColorToolbar`) renders a floating pill of swatch buttons — one per `NODE_COLORS` entry — positioned with `absolute bottom-full left-1/2 -translate-x-1/2 mb-2` on top of the node's own `relative` wrapper, so it sits just above the node without overlapping and re-centers automatically as the node resizes; carries `nodrag nopan` plus `onMouseDown`/`onClick` `stopPropagation` so clicking a swatch cannot start a node drag, pan the canvas, or bubble into a pane-click deselect. Each swatch is a small circle filled with the pair's fill color; the active swatch (matching the node's current `data.color`) gets a `2px` border plus a small always-on box-shadow in the pair's text color, inactive swatches get a subtle `var(--border-default)` border; hovering any swatch adds a tight `0 0 6px` box-shadow keyed to that swatch's own text color via an inline `--glow-color` CSS custom property (can't be expressed as a static Tailwind class since the color is per-swatch and data-driven). `canvas-node.tsx`: `CanvasNodeActions` context gains `updateColor(id, color)` alongside the existing `updateLabel`; `CanvasNodeRenderer` renders `<NodeColorToolbar activeColor={data.color} onSelect={...} />` only `{selected ? ... : null}`, wired to a `handleColorSelect` that calls `updateColor(id, pair.fill)`. `canvas.tsx` adds a `updateColor` callback mirroring `updateLabel`'s existing pattern exactly (look up the canonical node from `useLiveblocksFlow`'s `nodes`, dispatch `onNodesChange([{ id, type: "replace", item: { ...node, data: { ...node.data, color } } }])` — same reasoning as spec 14: nodes are Liveblocks-storage-controlled, so only `onNodesChange` writes through to the synced room) and passes it into `CanvasNodeActionsProvider`. Text color needed no separate update path — `CanvasNodeRenderer` already derives it every render via `getNodeTextColor(data.color)`, so changing only the stored fill color automatically repaints the label in its paired text color for every collaborator, live, with no server calls. Scope limits honored: no changes to drag/drop, no changes to node selection logic, no full color picker — palette is fixed to the 8 predefined pairs. `npm run build` and `npm run lint` pass.
- Feature spec 17: Presence avatars + cursors — `liveblocks.config.ts`'s `Presence.isThinking` renamed to `thinking` to match the field name the spec defines (`cursor: {x,y}|null`, `thinking: boolean`); `components/editor/canvas-room.tsx`'s `initialPresence` updated to match. New `components/editor/presence-avatars.tsx` (client) renders the collaborator group: reads `useOthers()` (Liveblocks) and `useUser()` (Clerk), filters out any entry whose `other.id` equals the current Clerk user ID (defensive/explicit per spec, even though `useOthers()` already excludes the local connection), renders up to 5 overlapping avatars (`-space-x-2`, `ring-2 ring-base` for the background-colored cutout separation that keeps overlapping avatars readable against the dark canvas) with a `+N` chip beyond that, a `bg-surface-border` divider shown only when at least one collaborator exists, and a same-sized (`h-8 w-8`, via `UserButton`'s `appearance.elements.userButtonAvatarBox`) Clerk `UserButton` last — display-only, no click handlers on the collaborator avatars. New `components/editor/live-cursors.tsx` (client) renders other participants' cursors as a `fixed inset-0` pointer-events-none overlay: reads `other.presence.cursor` (flow-space coordinates) and converts to viewport coordinates per-render via `useReactFlow().flowToScreenPosition` (subscribing to `useViewport()` to force a re-render — and thus a fresh conversion — on every pan/zoom tick, since `flowToScreenPosition`'s output depends on the current transform); pointer SVG + name badge both colored from `other.info.color` (the same deterministic per-user color `lib/liveblocks.ts` assigns at auth time), falling back to `var(--accent-primary)`/`"Anonymous"` if info is momentarily absent. `components/editor/canvas.tsx`: added `useMyPresence` (from `@liveblocks/react`) and `onPointerMove`/`onPointerLeave` handlers on the existing outer canvas div (same div that already had `onDragOver`/`onDrop`) that broadcast `{ cursor: screenToFlowPosition({x: clientX, y: clientY}) }` on move and `{ cursor: null }` on leave — chosen over Liveblocks' own prebuilt `@liveblocks/react-flow` `Cursors` component because that component resolves each cursor's name/color via `useUser()`, which requires a `resolveUsers` callback on `LiveblocksProvider` (unset here) — without it, `useUser()` returns `error: missingUserError` and renders with no name/color, silently failing the spec's "match pointer/badge color to presence color" requirement; hand-rolling against `other.info` (already present on the room's `others` array from the access-token `userInfo`, no extra resolver needed) avoids that gap. `<LiveCursors />` and `<PresenceAvatars />` added to `CanvasFlow`'s existing overlay stack alongside `<CanvasControlBar />`/`<ShapePanel />` (top-right corner, `z-20`/`z-30`, `pointer-events-none` wrappers with `pointer-events-auto` on the actual interactive bits — same pattern already used by those two components). `components/editor/editor-navbar.tsx` (used only by the editor home shell, `editor-shell.tsx`) was left untouched — it is already a separate component from `workspace-shell.tsx`'s inline canvas-room nav, so the presence UI naturally only appears in the canvas/room view per the spec's scope limit. `npm run build` and `npm run lint` pass.

- Feature spec 19: Canvas autosave — `app/api/projects/[projectId]/canvas/route.ts` (`PUT`/`GET`): both handlers require `getCurrentUserIdentity()` (401) and `getProjectWithAccess()` (403), matching the existing route pattern; `PUT` validates the body is `{ nodes: [], edges: [] }` (400 otherwise), uploads via `@vercel/blob`'s `put("canvas/{projectId}.json", ..., { access: "public", addRandomSuffix: false, allowOverwrite: true, contentType: "application/json" })` — a fixed, overwritable pathname per project (per `architecture-context.md`'s documented `canvas/{projectId}.json` pattern) rather than blob's default random-suffixed/versioned naming, since only the latest snapshot per project is ever needed — then writes the returned blob URL onto the existing `Project.canvasJsonPath` field (reused as-is from spec 05, no schema change); `GET` reads `canvasJsonPath` off the project record and re-fetches the JSON from that blob URL, returning `{ canvas: null }` when no save exists yet or the blob fetch fails. New `hooks/use-canvas-autosave.ts` (`useCanvasAutosave(projectId, nodes, edges)`): a `useEffect` keyed on `[projectId, nodes, edges]` clears any pending timeout and schedules a new 1.5s-debounced `PUT` on every nodes/edges change — since Liveblocks storage already gives referentially-stable arrays that only change identity on real mutations, no extra diffing was needed; a `skipFirstRef` guard no-ops the very first effect run (mount) so autosave never fires before the user makes any change, avoiding a redundant write of freshly-loaded (or genuinely empty) state; returns a `CanvasSaveStatus` (`"idle" | "saving" | "saved" | "error"`) for the caller to surface. `components/editor/canvas.tsx`: `CanvasFlow` now takes `projectId`/`onSaveStatusChange` props, calls `useCanvasAutosave` and forwards status changes up via a `useEffect`; a separate one-shot load effect (`hasAttemptedLoadRef` guard, deps intentionally limited to `[projectId]` since it must run exactly once per mount regardless of subsequent nodes/edges changes) checks `nodes.length > 0 || edges.length > 0` — since `useLiveblocksFlow`'s `suspense: true` guarantees storage is already resolved by first render, this check is race-free — and only when the room is empty does it `GET` the canvas route and replay the saved nodes/edges through `onNodesChange`/`onEdgesChange` as `"add"` changes (the same Storage-mutating path already used elsewhere in this file), per the spec's "skip the load entirely to avoid overwriting active collaboration" rule. `Canvas`/`CanvasRoom` (`canvas-room.tsx`) now thread `projectId`/`onSaveStatusChange` down from `WorkspaceShell`, which passes `roomId` as `projectId` (the room ID and project ID are the same value throughout this codebase — confirmed via `app/editor/[roomId]/page.tsx`, which calls `getProjectWithAccess(roomId, identity)` directly). `workspace-shell.tsx` adds a `saveStatus` state and a small `SaveStatusIndicator` (no prior "Save" button existed in the navbar, so one was added per the spec's step 5 — improvised as a status-only, non-interactive indicator rather than a manual-save trigger, since the spec only calls for autosave, not a manual save action) between the project name and the Share button: `Save`/`Loader2` (spinning)/`Check`/`AlertCircle` icons colored `text-copy-muted`/`text-copy-muted`/`text-success`/`text-error` for idle/saving/saved/error respectively, using the existing `--state-success`/`--state-error` tokens (`text-success`/`text-error` Tailwind utilities). `npm run build` and `npm run lint` pass.
- Feature spec 18: AI sidebar shell — new `components/editor/ai-sidebar.tsx` (`AISidebar({ isOpen, onClose })`, client) extracted the AI sidebar out of `workspace-shell.tsx`'s inline placeholder `<aside>` into its own floating component, following the same fixed-overlay + translate slide pattern already used by `project-sidebar.tsx` (`fixed top-12 right-0 h-[calc(100vh-3rem)] w-96 border-l border-surface-border bg-base/95 shadow-2xl transition-transform duration-300`, `translate-x-0`/`translate-x-full`) rather than the previous conditionally-rendered flex sibling, which had no slide animation and pushed the canvas width — matches `ui-context.md`'s documented layout pattern ("slide-over AI sidebar on the right" as a floating overlay, mirroring the left `ProjectSidebar`). Header: small `Bot` icon, `AI Workspace` title (`text-copy-primary`), `Collaborate with Ghost AI` subtitle (`text-copy-muted`), close button. Tabbed layout via shadcn `Tabs` (`AI Architect` / `Specs`); active tab styled `data-active:bg-ai data-active:text-ai-text` — resolved the spec's placeholder token names (`bg-accent`/`text-accent`/`text-accent-text`) to this project's actual `--accent-ai`/`--accent-ai-text` ("AI accent"/"AI text") tokens from `ui-context.md`, since no literal `accent`/`accent-text` Tailwind class exists in `globals.css`'s `@theme inline` mapping and the AI-specific token pair is the closest semantic match for an "AI Workspace" surface; inactive tab text `text-copy-muted`. AI Architect tab: `ScrollArea`-based scrollable message list; empty state (`Bot` icon, description, three starter-prompt pill buttons styled `bg-subtle text-ai-text` that populate a message when clicked); user messages right-aligned `bg-brand-dim border-2 border-brand/50 text-copy-primary`; assistant messages left-aligned `bg-elevated border border-surface-border text-ai-text`; `Textarea` input (`min-h-[72px] max-h-[160px]`, auto-resizes via the shadcn `Textarea`'s existing `field-sizing-content` base class — no extra JS resize logic needed) with `Enter` submits / `Shift+Enter` newline, send button `bg-ai text-white`. Sending a message locally appends the user's text plus one hardcoded placeholder assistant reply ("Ghost AI generation isn't wired up yet.") purely to demonstrate both message stylings — no AI call, no backend, matching the spec's Specs-tab "demo card for now" precedent and the explicit "don't add AI generation logic yet" scope limit. Specs tab: `Generate Spec` button (`bg-ai text-white`, no handler) above a static demo spec card (`bg-elevated border-surface-border`, `FileText` icon, demo title/snippet, disabled `Download` button with `Download` icon). `workspace-shell.tsx` now renders `<AISidebar isOpen={isAISidebarOpen} onClose={...} />` as a fixed-position sibling inside the canvas row (same slot the old inline `<aside>` occupied) instead of a flex child — open/close state ownership (`isAISidebarOpen` in `WorkspaceShell`) and the navbar toggle button were left untouched per scope limits. `npm run build` and `npm run lint` pass (lint's 2 warnings are the same pre-existing/unrelated ones in `lib/liveblocks.ts`/`lib/prisma.ts` noted under spec 16).

- Fix (`context/current-issues.md`): selected nodes/edges couldn't be deleted. New `hooks/use-canvas-delete.ts` (`useCanvasDelete({ onDelete })`) reads live selection state via `@xyflow/react`'s `useNodes()`/`useEdges()` and attaches a single `window` `keydown` listener (same `isEditableTarget` input/textarea/contenteditable guard pattern as `use-keyboard-shortcuts.ts`, duplicated rather than shared to avoid touching that unrelated file): on `Delete`/`Backspace`, filters to `selected` nodes/edges and calls the existing `onDelete` mutation from `useLiveblocksFlow`. First attempt wrongly dispatched `{ id, type: "remove" }` through `onNodesChange`/`onEdgesChange` — traced (after the user reported delete still not working) to `@liveblocks/react-flow`'s `flow.js`, where `applyNodeChanges`/`applyEdgeChanges` treat the `"remove"` change type as a no-op (`case "remove": break;`); the library only removes storage entries through the dedicated `onDelete` mutation, which takes `{ nodes, edges }` arrays of full node/edge objects and deletes them from the underlying `LiveMap`s directly — switched the hook to call `onDelete({ nodes: selectedNodes, edges: selectedEdges })` instead. `components/editor/canvas.tsx`: calls the hook from `CanvasFlow` with the same `onDelete` already destructured from `useLiveblocksFlow` and passed to `<ReactFlow onDelete={onDelete}>`; `<ReactFlow>` also sets `deleteKeyCode={null}` to fully disable React Flow's own built-in keyboard deletion path per the issue's explicit instruction.
- Fix (`context/current-issues.md`): removed `<UserButton />` from the workspace navbar (`components/editor/workspace-shell.tsx`'s inline `<nav>`) — the editor home navbar (`components/editor/editor-navbar.tsx`, used only by `editor-shell.tsx`) keeps its own `<UserButton />` untouched. Note: the issue described "the navbar" as shared between the two contexts requiring a conditional-render prop, but the codebase already has two separate navbar implementations (workspace-shell.tsx's inline nav vs. the standalone `EditorNavbar` component) — no shared component exists, so no conditional logic was needed to achieve the stated outcome (`UserButton` gone from workspace, present on editor home).
- `npm run build` and `npm run lint` pass for both fixes above (lint's 2 warnings are the same pre-existing/unrelated ones in `lib/liveblocks.ts`/`lib/prisma.ts`).

## In Progress

- None.

## Next Up

- Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session for feature spec 19 (canvas autosave): editing the canvas (add/move/delete a node, draw an edge) shows "Saving…" then settles on "Saved" ~1.5s after the last change, without firing on every keystroke; reloading the page for a project with saved content loads the saved nodes/edges back onto the canvas; opening the same project in a second tab/session while the first still has live content does NOT get overwritten by a load (room-not-empty skip); killing the network (or an invalid/missing `BLOB_READ_WRITE_TOKEN`) shows "Save failed" in red; a brand-new project with an empty canvas shows no false "Save failed" on first load.
- Not verified interactively in a browser (same environment caveat as prior specs) — ask the user to confirm in their own session: the AI sidebar slides in smoothly from the right without pushing/resizing the canvas, the header/tabs/empty-state/starter-chip styling and colors read as intended, sending a message shows both the user (cyan/brand) and assistant (AI-accent) message stylings, the textarea grows between 72px and 160px as text is typed and stops growing beyond that, `Enter` sends while `Shift+Enter` inserts a newline, and the Specs tab's demo card and disabled Download button render correctly.
- Move to the next feature spec in `context/feature-specs/`.
- Manually verify feature spec 17 in a real browser session with two signed-in sessions: the collaborator avatar group appears top-right of the canvas only (never on the editor home navbar), shows profile photos or initials, overlaps up to 5 with a `+N` chip beyond that, shows the divider only when a collaborator is present, and the Clerk `UserButton` renders at the same size as the collaborator avatars; live cursors appear for other participants only (never your own), track pan/zoom correctly, and their pointer/badge color matches each participant's assigned presence color.
- Manually verify in a real browser session: each of the 6 shapes renders correctly on the canvas (not just rectangles), selected nodes show a brighter border, dragging a shape from the panel shows a ghost preview matching that shape/size attached to the cursor that disappears on drop or cancel, hovering a node reveals its 4 connection handles which can be dragged to another node to create an edge, selected nodes show resize handles that enforce the minimum size and persist after reselecting, double-click label editing (live typing, blur-to-commit, Escape-to-cancel, no drag/pan leakage) behaves as specified, and selecting a node shows the floating color toolbar just above it (swatch click updates both fill and text color immediately, active swatch is visually distinct, hover shows a tight glow, and clicking a swatch never drags the node or pans/deselects the canvas).
- Manually verify feature spec 16 (control bar + keyboard shortcuts) per the checklist noted under that spec above.
- Set `LIVEBLOCKS_SECRET_KEY` in `.env.local` (and deployment env) — the canvas wrapper is wired up but the auth route is unauthenticated against the real Liveblocks API until this is configured, so live multi-user sync/cursors can't be verified locally yet.

## Open Questions

- None yet.

## Architecture Decisions

- Dark-only theme: all shadcn component variables set in `:root` to dark values — no `.dark` class needed.
- CSS custom properties in globals.css serve two layers: shadcn variables (--background, --primary, etc.) and project tokens (--bg-base, --accent-primary, etc.).
- Tailwind @theme inline maps both layers to utility classes: bg-base, text-copy-primary, border-surface-border, text-brand, bg-brand-dim, etc.
- shadcn/ui components live in components/ui/ and must not be modified after generation.

## Session Notes

- Project uses Next.js 16.2.9, React 19, Tailwind v4, TypeScript strict mode.
- Path alias @/* maps to project root.
- VS Code shows @apply warnings in globals.css — these are CSS lint false positives, not build errors.
